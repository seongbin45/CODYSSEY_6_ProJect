# 되나요 (Doenayo) — 구현 골격

> 이 문서의 방침
> - **완성형으로 제공**: `api/generate.py`, `js/app.js`, `SYSTEM_PROMPT` → 틀리면 원인 찾기 어렵고, 함정이 몰려 있는 부분
> - **골격 + TODO로 제공**: `index.html`, `css/style.css` → 본인 AI 코딩 도구로 채우면서 **대화 로그를 남길 부분** (증빙 자료가 여기서 나온다)
>
> 완성된 사이트를 통째로 받으면 과제 목표 6번("오류 원인을 파악하고 수정 방향을 말로 설명")을 방어할 수 없다. HTML/CSS는 반드시 직접 채운다.

---

## 0. 파일 생성 순서

계획서 Phase 1과 동일하되, `.gitignore` 커밋이 **반드시 첫 번째**다.

```bash
mkdir doenayo && cd doenayo
git init

cat > .gitignore <<'EOF'
.env
.env.local
.venv/
venv/
__pycache__/
*.pyc
.vercel/
.DS_Store
EOF

git add .gitignore
git commit -m "chore: add gitignore before anything else"

mkdir -p css js api images
echo "google-genai" > requirements.txt
echo "GEMINI_API_KEY=" > .env.example
echo "GEMINI_API_KEY=발급받은_키" > .env      # 커밋되지 않음
```

> ⚠️ `requirements.txt` — **소문자**, 내용은 **`google-genai` 한 줄만**.
> 대문자 `Requirements.txt`는 Vercel이 못 찾고, `flask`/`fastapi`가 들어가면 `api/`가 통째로 무효화된다. (진단서 §3-1 참고)

---

## 1. SYSTEM_PROMPT — AI 기능의 핵심

`api/generate.py` 안에 상수로 둔다. **이 서비스의 품질은 거의 전부 이 문자열이 결정한다.**

```python
SYSTEM_PROMPT = """당신은 청년 정책 공고문을 쉬운 말로 풀어주는 해설자입니다.
사용자가 붙여넣은 공고문 텍스트를 읽고, 아래 규칙에 따라 JSON만 출력하세요.

# 절대 규칙
1. 입력된 텍스트에 실제로 적혀 있는 내용만 사용합니다.
2. 원문에 없는 정보는 절대 만들어내지 마세요. 일반 상식이나 다른 정책 지식으로
   빈칸을 채우지 마세요.
3. 원문에서 확인할 수 없는 항목은 "공고문에 명시되지 않음" 이라고 적으세요.
4. 지원 자격을 최종 판정하지 마세요. "대상입니다" / "해당됩니다" 같은 단정 표현을
   쓰지 말고, 사용자가 스스로 확인할 수 있도록 조건을 항목으로 분해만 하세요.
5. 입력이 정책·지원사업 공고문이 아니면 is_policy 를 false 로 하고
   나머지 필드는 빈 배열 또는 빈 문자열로 두세요.

# 문체
- 존댓말, 행정 용어 대신 일상어를 사용합니다.
- 각 요약 문장은 45자 이내로 씁니다.
- 어려운 용어가 나오면 terms 에 풀이를 넣습니다. 최대 4개.

# 출력 형식
아래 구조의 JSON 객체 하나만 출력합니다.
설명 문장, 인사말, 마크다운 코드펜스(```) 를 절대 붙이지 마세요.

{
  "is_policy": true 또는 false,
  "title": "공고문에서 파악한 사업명 (없으면 빈 문자열)",
  "summary": ["무슨 사업인지", "무엇을 얼마나 지원하는지", "누가 어떻게 신청하는지"],
  "eligibility": [
    {"item": "조건을 한 문장으로", "note": "판단 기준일이나 예외. 없으면 빈 문자열"}
  ],
  "documents": ["필요한 서류명"],
  "deadline": "신청 기한. 원문에 없으면 공고문에 명시되지 않음",
  "terms": [
    {"word": "용어", "meaning": "한 문장 풀이"}
  ]
}

# 항목별 지침
- summary: 정확히 3개.
- eligibility: 원문의 자격 조건을 하나씩 분해합니다. 최대 8개.
  "만 19~34세", "군산시 거주", "기준 중위소득 150% 이하" 처럼
  사용자가 O/X 로 답할 수 있는 단위로 쪼개세요.
- documents: 본문과 별첨에 흩어진 서류를 모두 모읍니다. 원문에 없으면 빈 배열.
- deadline: 날짜와 시각까지 원문 그대로 옮깁니다.
"""
```

### 설계 의도 (구술 방어용)

| 규칙 | 왜 넣었나 |
|---|---|
| "원문에 없는 정보는 만들지 마라" + "명시되지 않음" | 정책 도메인에서 환각은 실제 피해(잘못된 서류 준비, 마감 착오)로 이어진다. 빈칸을 **빈칸으로 두는 것**이 지어내는 것보다 낫다 |
| "자격을 판정하지 마라" | AI는 사용자의 나이·소득·주소를 모른다. 판정하면 근거 없는 단정이 된다. 조건 분해까지만 하고 판단은 사용자에게 넘긴다 |
| `is_policy` 플래그 | 아무 텍스트나 붙여넣었을 때 억지 결과를 만들지 않고 안내로 전환하기 위함 |
| "코드펜스를 붙이지 마라" | 모델이 ```json 으로 감싸는 습관이 있어 파싱이 깨진다. 프롬프트로 1차 차단하고, 코드에서 2차로 제거한다 |
| "O/X 로 답할 수 있는 단위로 쪼개라" | 결과를 체크박스로 렌더링하기 위한 형식 요구. 출력 형태가 UI 설계에서 역산된다 |

---

## 2. `api/generate.py` — 완성형

```python
from http.server import BaseHTTPRequestHandler
import json
import os
import re

from google import genai

# ── 설정 ──────────────────────────────────────────────
MODEL = "gemini-3.6-flash"      # ⚠️ AI Studio에서 현재 사용 가능한 모델명으로 교체
MIN_INPUT_LEN = 10
MAX_INPUT_LEN = 2000

SYSTEM_PROMPT = """(위 1번의 프롬프트를 그대로 붙여넣습니다)"""

REQUIRED_KEYS = ("is_policy", "summary", "eligibility", "documents", "deadline")


# ── 유틸 ──────────────────────────────────────────────
def extract_json(text):
    """모델 응답에서 JSON 객체만 뽑아낸다.
    프롬프트로 코드펜스를 막아도 가끔 붙으므로 2차 방어."""
    t = (text or "").strip()

    fenced = re.search(r"```(?:json)?\s*(.*?)```", t, re.S)
    if fenced:
        t = fenced.group(1).strip()

    start, end = t.find("{"), t.rfind("}")
    if start == -1 or end == -1 or end < start:
        raise ValueError("응답에서 JSON 객체를 찾지 못했습니다.")

    return json.loads(t[start:end + 1])


def normalize(data):
    """필드 누락에 대비해 기본값을 채운다. 프론트 렌더링이 터지지 않게."""
    if not isinstance(data, dict):
        raise ValueError("JSON 객체가 아닙니다.")

    return {
        "is_policy":   bool(data.get("is_policy", False)),
        "title":       str(data.get("title") or ""),
        "summary":     [str(s) for s in (data.get("summary") or [])][:3],
        "eligibility": [
            {"item": str(e.get("item", "")), "note": str(e.get("note", "") or "")}
            for e in (data.get("eligibility") or []) if isinstance(e, dict)
        ][:8],
        "documents":   [str(d) for d in (data.get("documents") or [])],
        "deadline":    str(data.get("deadline") or "공고문에 명시되지 않음"),
        "terms": [
            {"word": str(t.get("word", "")), "meaning": str(t.get("meaning", ""))}
            for t in (data.get("terms") or []) if isinstance(t, dict)
        ][:4],
    }


# ── 핸들러 ────────────────────────────────────────────
class handler(BaseHTTPRequestHandler):

    def _send(self, status, payload):
        body = json.dumps(payload, ensure_ascii=False).encode("utf-8")
        self.send_response(status)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def do_POST(self):
        # ① 요청 파싱
        try:
            length = int(self.headers.get("Content-Length") or 0)
            raw = self.rfile.read(length) if length else b"{}"
            data = json.loads(raw.decode("utf-8") or "{}")
        except (ValueError, UnicodeDecodeError):
            self._send(400, {"error": "요청 형식이 올바르지 않습니다."})
            return

        # ② 입력 검증 (기획서 §4-6 의 1·2·3번)
        text = (data.get("input") or "").strip()
        if not text:
            self._send(400, {"error": "공고문 내용을 붙여넣어 주세요."})
            return
        if len(text) < MIN_INPUT_LEN:
            self._send(400, {"error": "내용이 너무 짧습니다. 지원 자격이나 서류 부분을 함께 붙여넣어 주세요."})
            return
        if len(text) > MAX_INPUT_LEN:
            self._send(400, {"error": f"{MAX_INPUT_LEN:,}자까지 입력할 수 있습니다. 지원 자격·서류·기한 부분만 붙여넣어 보세요."})
            return

        # ③ 환경변수 확인 (6번)
        if not os.environ.get("GEMINI_API_KEY"):
            print("CONFIG_ERROR: GEMINI_API_KEY is missing")
            self._send(500, {"error": "서비스 점검 중입니다. 잠시 후 다시 시도해 주세요."})
            return

        # ④ AI 호출 (4번)
        try:
            client = genai.Client()          # GEMINI_API_KEY 자동 인식
            interaction = client.interactions.create(
                model=MODEL,
                system_instruction=SYSTEM_PROMPT,
                input=text,
            )
            output = interaction.output_text
        except Exception as e:
            print("GEMINI_ERROR:", repr(e))
            self._send(502, {"error": "결과를 가져오지 못했습니다. 잠시 후 다시 시도해 주세요."})
            return

        # ⑤ 응답 파싱 (5번)
        try:
            result = normalize(extract_json(output))
        except (ValueError, json.JSONDecodeError) as e:
            print("PARSE_ERROR:", repr(e), "| RAW:", (output or "")[:300])
            self._send(502, {"error": "결과를 정리하지 못했습니다. 내용을 조금 줄여서 다시 시도해 주세요."})
            return

        self._send(200, {"result": result})
```

### 이 파일에서 반드시 이해하고 넘어갈 것

| 코드 | 의미 |
|---|---|
| `class handler(BaseHTTPRequestHandler)` | Vercel 파일 기반 Python 함수가 인식하는 최상위 이름은 `app` / `application` / `handler` 셋뿐. 프레임워크 없이 쓸 수 있는 건 `handler`. **소문자 h** |
| 파일 위치 = 라우트 | `api/generate.py` → `/api/generate` |
| `do_POST` | GET이 아닌 POST만 처리. GET으로 접속하면 501이 난다 (정상 동작) |
| `print(...)` | 표준 출력은 Vercel **Runtime Logs**로 간다. 사용자에게는 안 보인다. 디버깅 통로 |
| 예외 원문을 응답에 넣지 않음 | 키·내부 경로 유출 방지 |
| `normalize()` | AI 응답의 필드가 빠져도 프론트가 터지지 않게 하는 방어층. **AI 출력은 신뢰할 수 없는 입력으로 취급한다** |

---

## 3. `js/app.js` — 완성형

```js
// ── 설정 ──────────────────────────────────────────────
const TIMEOUT_MS = 25000;
const MIN_LEN = 10;
const MAX_LEN = 2000;

// ── DOM ───────────────────────────────────────────────
const form    = document.getElementById('check-form');
const input   = document.getElementById('policy-input');
const counter = document.getElementById('char-count');
const btn     = document.getElementById('submit-btn');
const notice  = document.getElementById('notice');
const result  = document.getElementById('result');
const sampleBtn = document.getElementById('sample-btn');

// ── 글자수 카운터 ──────────────────────────────────────
input.addEventListener('input', () => {
  const n = input.value.trim().length;
  counter.textContent = `${n.toLocaleString()} / ${MAX_LEN.toLocaleString()}자`;
  counter.classList.toggle('over', n > MAX_LEN);
});

// ── 통신 ──────────────────────────────────────────────
async function requestSummary(text) {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), TIMEOUT_MS);

  try {
    const res = await fetch('/api/generate', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ input: text }),
      signal: controller.signal,
    });

    const data = await res.json().catch(() => ({}));

    if (!res.ok) {
      // 서버가 내려준 안내 문구를 그대로 사용 (400 / 500 / 502)
      throw new Error(data.error || '요청을 처리하지 못했습니다.');
    }
    return data.result;

  } catch (err) {
    if (err.name === 'AbortError') {
      throw new Error('응답이 지연되고 있습니다. 잠시 후 다시 시도해 주세요.');
    }
    if (err instanceof TypeError) {
      throw new Error('네트워크 연결을 확인해 주세요.');
    }
    throw err;

  } finally {
    clearTimeout(timer);
  }
}

// ── 렌더링 ────────────────────────────────────────────
function esc(s) {
  return String(s).replace(/[&<>"']/g, c => (
    { '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[c]
  ));
}

function render(r) {
  if (!r.is_policy) {
    notice.textContent =
      '정책 공고문으로 보이지 않습니다. 지원 자격이나 신청 방법이 담긴 부분을 붙여넣어 주세요.';
    return;
  }

  const block = (title, inner) =>
    `<section class="result-block"><h3>${esc(title)}</h3>${inner}</section>`;

  const html = [
    r.title ? `<p class="result-title">${esc(r.title)}</p>` : '',

    block('한눈에 보기',
      `<ol class="summary">${r.summary.map(s => `<li>${esc(s)}</li>`).join('')}</ol>`),

    block('내가 대상인지 확인하기',
      r.eligibility.length
        ? `<ul class="checklist">${r.eligibility.map((e, i) => `
            <li>
              <input type="checkbox" id="elig-${i}">
              <label for="elig-${i}">${esc(e.item)}
                ${e.note ? `<span class="note">${esc(e.note)}</span>` : ''}
              </label>
            </li>`).join('')}</ul>`
        : '<p class="empty">공고문에서 자격 조건을 찾지 못했습니다.</p>'),

    block('준비할 서류',
      r.documents.length
        ? `<ul class="docs">${r.documents.map(d => `<li>${esc(d)}</li>`).join('')}</ul>`
        : '<p class="empty">공고문에 명시되지 않음</p>'),

    block('신청 기한', `<p class="deadline">${esc(r.deadline)}</p>`),

    r.terms.length
      ? block('어려운 말 풀이',
          `<dl class="terms">${r.terms.map(t =>
            `<dt>${esc(t.word)}</dt><dd>${esc(t.meaning)}</dd>`).join('')}</dl>`)
      : '',

    `<p class="disclaimer">⚠️ 이 결과는 AI가 붙여넣은 텍스트만 보고 정리한 참고 자료입니다.
      최종 자격과 서류는 반드시 공고 원문과 담당 기관을 통해 확인하세요.</p>`,
  ].join('');

  result.innerHTML = html;
}

// ── 상태 전환 ─────────────────────────────────────────
function setLoading(on) {
  btn.disabled = on;
  btn.textContent = on ? '정리하는 중...' : '결과 확인하기';
  if (on) { notice.textContent = ''; result.innerHTML = ''; }
}

// ── 제출 ──────────────────────────────────────────────
form.addEventListener('submit', async (e) => {
  e.preventDefault();

  const text = input.value.trim();

  // 클라이언트 1차 검증 — 서버 왕복 없이 즉시 안내 (무료 쿼터 절약)
  if (!text) {
    notice.textContent = '공고문 내용을 붙여넣어 주세요.';
    input.focus();
    return;
  }
  if (text.length < MIN_LEN) {
    notice.textContent = '내용이 너무 짧습니다. 지원 자격이나 서류 부분을 함께 붙여넣어 주세요.';
    input.focus();
    return;
  }
  if (text.length > MAX_LEN) {
    notice.textContent = `${MAX_LEN.toLocaleString()}자까지 입력할 수 있습니다.`;
    input.focus();
    return;
  }

  setLoading(true);
  try {
    render(await requestSummary(text));
  } catch (err) {
    notice.textContent = err.message;
  } finally {
    setLoading(false);
  }
});

// ── 샘플 넣어보기 ─────────────────────────────────────
sampleBtn.addEventListener('click', () => {
  input.value = SAMPLE_TEXT;          // TODO: 아래 5번 참고하여 정의
  input.dispatchEvent(new Event('input'));
  input.focus();
});

// ── 홈 CTA → 입력창 포커스 ────────────────────────────
document.querySelectorAll('[data-goto-check]').forEach(el => {
  el.addEventListener('click', () => setTimeout(() => input.focus(), 400));
});
```

### 여기서 짚을 것

| 코드 | 이유 |
|---|---|
| `esc()` | AI 응답을 `innerHTML`에 넣으므로 **XSS 방어가 필수**다. AI 출력을 신뢰하지 않는다는 원칙의 프론트 버전 |
| `AbortController` | Vercel Hobby 함수 한도는 300초라 서버는 안 끊는다. **지연 안내는 클라이언트가 만들어야 한다** |
| `err instanceof TypeError` | `fetch`는 네트워크 실패 시 `TypeError`를 던진다. 상태 코드 오류(4xx/5xx)와 구분해야 안내 문구가 달라진다 |
| `btn.disabled` | 연타 방지 = 무료 쿼터 방어 |
| 서버·클라 양쪽 검증 | 클라는 UX용, 서버는 보안용. `curl`로 보내면 클라 검증은 무력화됨 |

---

## 4. `index.html` — 골격 (여기부터 직접 채운다)

`app.js`가 참조하는 **id는 고정**이다. 나머지 마크업·문구·구조는 자유롭게 확장한다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>되나요 — 청년 정책 공고문, 대상 여부부터 확인하세요</title>
  <meta name="description" content="청년 정책 공고문을 붙여넣으면 3줄 요약과 자격 체크리스트, 준비 서류, 신청 기한으로 정리해 드립니다.">
  <link rel="stylesheet" href="css/style.css">
</head>
<body>

  <!-- ══ 헤더 / 네비게이션 ══════════════════════════ -->
  <header class="site-header">
    <nav class="nav">
      <a href="#home" class="logo">되나요</a>

      <!-- TODO: 모바일 햄버거 버튼. aria-expanded 토글까지 구현할 것 -->
      <button class="hamburger" aria-label="메뉴 열기" aria-expanded="false">☰</button>

      <ul class="nav-links">
        <li><a href="#home">홈</a></li>
        <li><a href="#check">확인하기</a></li>
        <li><a href="#guide">이용 안내</a></li>
      </ul>
    </nav>
  </header>

  <main>

    <!-- ══ 섹션 1: 홈 ══════════════════════════════ -->
    <section id="home" class="section hero">
      <h1>그래서, 제가 되나요?</h1>
      <!-- TODO: 문제 제기 3줄. 아래는 방향만. 본인 문장으로 다시 쓸 것 -->
      <p class="lead">
        청년 정책 공고문은 조건이 겹겹이 붙어 있어,
        끝까지 읽어도 내가 대상인지 알기 어렵습니다.
      </p>
      <a href="#check" class="btn-primary" data-goto-check>공고문 붙여넣고 확인하기</a>

      <!-- TODO: 3단계 사용법을 아이콘/카드로 (붙여넣기 → 확인 → 서류 준비) -->
    </section>

    <!-- ══ 섹션 2: 확인하기 (AI 기능) ═══════════════ -->
    <section id="check" class="section">
      <h2>공고문 확인하기</h2>
      <p class="section-desc">
        지원 자격, 제출 서류, 신청 기한이 담긴 부분을 붙여넣어 주세요.
      </p>

      <form id="check-form">
        <label for="policy-input" class="sr-only">공고문 내용</label>
        <textarea id="policy-input" rows="8"
                  placeholder="예: 지원 대상 - 공고일 기준 군산시에 주민등록을 둔 만 19세~34세 청년으로서 ..."></textarea>

        <div class="form-meta">
          <span id="char-count">0 / 2,000자</span>
          <button type="button" id="sample-btn" class="btn-ghost">샘플 넣어보기</button>
        </div>

        <button type="submit" id="submit-btn" class="btn-primary">결과 확인하기</button>
      </form>

      <!-- 실패 안내. role=alert 로 스크린리더에 즉시 전달 -->
      <div id="notice" class="notice" role="alert" aria-live="assertive"></div>

      <!-- 결과 4블록이 여기에 렌더링됨 -->
      <div id="result" class="result" aria-live="polite"></div>
    </section>

    <!-- ══ 섹션 3: 이용 안내 ════════════════════════ -->
    <section id="guide" class="section">
      <h2>이용 안내</h2>

      <!-- TODO: 사용법 3단계 서술 -->

      <!-- TODO: AI 요약의 한계 고지. 아래 내용을 본인 문장으로 -->
      <!--   · 붙여넣은 텍스트만 보고 정리하므로 원문 전체와 다를 수 있음 -->
      <!--   · 자격 여부를 판정하지 않음. 조건을 분해해 줄 뿐 -->
      <!--   · 최종 확인은 공고 원문과 담당 기관 -->

      <!-- TODO: 자주 나오는 용어 풀이 (중위소득, 가구원, 소득분위 등) -->
      <!-- TODO: 공식 출처 링크 (온통청년, 군산시청 청년정책 등) -->
    </section>

  </main>

  <footer class="site-footer">
    <!-- TODO: 서비스명, 제작 목적(학습용 프로젝트임을 명시), 만든 사람 -->
  </footer>

  <script src="js/app.js"></script>
</body>
</html>
```

### 고정해야 할 id (변경 시 `app.js`도 함께 고쳐야 함)

`check-form` · `policy-input` · `char-count` · `submit-btn` · `notice` · `result` · `sample-btn`

---

## 5. 샘플 공고문 (`SAMPLE_TEXT`)

`js/app.js` 최상단에 상수로 둔다. **아래는 형식 예시일 뿐이다. 실제 군산시·전북도 청년 정책 공고문을 찾아 그 텍스트로 교체할 것.** 가짜 공고문을 그대로 두면 결과의 신뢰도도 떨어지고, 도메인 조사를 했다는 근거도 안 남는다.

```js
const SAMPLE_TEXT = `[○○ 청년 △△ 지원사업 모집 공고]

1. 지원 대상
  - 공고일 기준 ○○시에 주민등록을 둔 만 19세 이상 34세 이하 청년
  - 기준 중위소득 150% 이하 가구의 구성원
  - 다만, 동일 사업에 기(旣) 참여한 자는 제외한다

2. 지원 내용
  - 월 ○○만원, 최대 ○개월

3. 제출 서류
  - 신청서(별지 제1호 서식), 신분증 사본, 주민등록등본
  - 건강보험료 납부확인서 (별첨 참조)

4. 신청 기간
  - 2026. ○. ○.(월) 09:00 ~ 2026. ○. ○.(금) 18:00

5. 문의
  - ○○시청 청년정책과 (000-000-0000)`;
```

---

## 6. `css/style.css` — 토큰만 제공, 나머지는 직접

디자인은 직접 하되, **CSS 변수 구조만은 이대로 시작**한다. 보너스 과제(다크 모드)가 30분 작업이 된다.

```css
:root {
  /* 색 — TODO: 본인 서비스 톤으로 교체 */
  --c-bg:      #ffffff;
  --c-surface: #f6f7f9;
  --c-text:    #16181d;
  --c-muted:   #6b7280;
  --c-border:  #e3e6ea;
  --c-primary: #2563eb;
  --c-danger:  #dc2626;

  /* 간격 */
  --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px;
  --sp-4: 16px; --sp-6: 24px; --sp-8: 32px; --sp-12: 48px;

  --radius: 10px;
  --header-h: 60px;
  --maxw: 1080px;
}

*, *::before, *::after { box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  margin: 0;
  background: var(--c-bg);
  color: var(--c-text);
  font-family: system-ui, -apple-system, "Apple SD Gothic Neo",
               "Malgun Gothic", sans-serif;
  line-height: 1.7;
  word-break: keep-all;          /* 한글 어절 단위 줄바꿈 */
}

img { max-width: 100%; height: auto; }

.site-header { position: sticky; top: 0; z-index: 10;
               height: var(--header-h); background: var(--c-bg);
               border-bottom: 1px solid var(--c-border); }

.section { scroll-margin-top: var(--header-h); }   /* 앵커 이동 시 제목 가림 방지 */

/* 입력 요소는 16px 이상 — iOS에서 포커스 시 확대 방지 */
textarea, input, button { font: inherit; font-size: 16px; }

textarea { width: 100%; overflow-wrap: anywhere; }

/* 스크린리더 전용 텍스트 */
.sr-only { position: absolute; width: 1px; height: 1px;
           padding: 0; margin: -1px; overflow: hidden;
           clip: rect(0 0 0 0); white-space: nowrap; border: 0; }

/* ── 모바일 우선. 아래부터 직접 채운다 ─────────────── */
/* TODO: .nav, .hamburger, .nav-links 토글 */
/* TODO: .hero, .btn-primary, .btn-ghost */
/* TODO: .notice (--c-danger 사용), .result-block, .checklist, .terms */
/* TODO: .disclaimer */

@media (min-width: 768px) {
  /* TODO: 최대폭 720px 중앙 정렬, 상단 메뉴 노출, 햄버거 숨김 */
}

@media (min-width: 1024px) {
  /* TODO: 최대폭 var(--maxw) */
}
```

---

## 7. AI 코딩 도구에게 시킬 작업 목록

아래를 순서대로 시킨다. **매 대화 시작 시 계획서 Phase 0-3의 제약 프롬프트를 먼저 붙여넣는다.**
각 단계의 대화가 곧 증빙 자료가 되므로, 결과를 그냥 받지 말고 **"왜 그렇게 했는지" 를 되묻고 그 답도 로그에 남긴다.**

| # | 시킬 것 | 확인할 것 |
|---|---|---|
| 1 | `index.html`의 TODO 중 **홈 섹션** 문구와 3단계 사용법 카드 작성 | 시맨틱 태그를 썼는가. `<div>` 도배 아닌가 |
| 2 | 모바일 **햄버거 메뉴** 토글 (`aria-expanded` 포함) | 키보드로도 열리는가 |
| 3 | `css/style.css`의 TODO 구간 — 모바일 우선 스타일 | 미디어쿼리가 `min-width` 기준인가 |
| 4 | **이용 안내 섹션** — 한계 고지와 용어 풀이 | 문구가 기획서 §4-5와 일치하는가 |
| 5 | 768px / 1024px 브레이크포인트 대응 | 375px에서 가로 스크롤이 없는가 |
| 6 | (보너스) 다크 모드 — `[data-theme="dark"]` 오버라이드 | `prefers-color-scheme`를 초기값으로 쓰는가 |

> **AI가 이런 걸 제안하면 즉시 거부한다**
> `flask`/`fastapi`를 `requirements.txt`에 추가 · React/Vue 도입 · Tailwind CDN 추가 ·
> `genai.configure()` / `GenerativeModel` / `response.text` 사용 · API 키를 JS에 하드코딩

---

## 8. 배포 전 최종 점검

```bash
# ① 키가 커밋 이력에 없는지
git log -p | grep -iE "AIza|api[_-]?key\s*=\s*['\"][A-Za-z0-9_-]{20,}"

# ② requirements.txt 확인 (소문자 파일명, google-genai 한 줄)
cat requirements.txt && ls -1 | grep -i requirements

# ③ .env 가 추적되지 않는지
git status --porcelain | grep -i "\.env$"     # 아무것도 안 나와야 정상
```

**Vercel 설정**
- Framework Preset: **Other**
- Environment Variables: `GEMINI_API_KEY` → **Production / Preview / Development 전부 체크**
- 환경변수 추가·수정 후에는 **반드시 Redeploy**

**배포 URL에서 확인 (localhost 아님)**
- ☐ 3개 섹션 네비 이동
- ☐ 샘플 넣어보기 → 결과 4블록 표시
- ☐ 빈 입력 → 안내 문구
- ☐ `TIMEOUT_MS`를 100으로 임시 변경 → 지연 안내 (확인 후 원복)
- ☐ 환경변수 임시 삭제 → 점검 중 안내 (확인 후 원복)
- ☐ 정책과 무관한 텍스트 붙여넣기 → `is_policy: false` 안내
- ☐ 375px 모바일 표시
- ☐ 시크릿 창 / 다른 기기에서 접속

---

## 9. 다음 단계

Phase 1(워킹 스켈레톤) 진행 후, 배포 URL이 나오면 알려주세요.
그 시점에 **README.md 초안**과 **증빙 스크린샷 촬영 순서**를 정리해 드리겠습니다.
