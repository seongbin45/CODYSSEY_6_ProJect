# 되나요 (Doenayo) — 서비스 기획서

> 청년 정책 공고문을 붙여넣으면, 대상 여부부터 확인해 주는 웹 서비스
> 작성일: 2026-08-14 · 작성자: 최성빈

---

## 1. 서비스 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | **되나요 (Doenayo)** |
| 한 줄 소개 | 청년 정책 공고문을 붙여넣으면 3줄 요약 · 자격 체크리스트 · 준비 서류 · 신청 기한으로 정리해 주는 서비스 |
| 배포 URL | (Vercel 배포 후 기입) |
| 핵심 질문 | "그래서, 제가 되나요?" |

### 1-1. 해결하려는 문제

청년 정책 공고문은 **정보가 없어서가 아니라 읽히지 않아서** 신청으로 이어지지 않는다.

- 공고문은 행정 문서 형식이라 지원 자격이 `~에 해당하는 자로서, 다만 ~인 경우는 제외한다` 같은 중첩 조건문으로 서술된다
- 정작 사용자가 알고 싶은 건 하나다 — **"내가 대상인가"**. 그런데 그 답은 공고문 어디에도 한 줄로 적혀 있지 않다
- 준비 서류는 본문 중간과 별첨 양식에 흩어져 있어, 신청 마감 직전에야 빠진 서류를 발견한다
- 군산·전북 청년정책 공고를 직접 열어 보니, **상세 내용이 웹 본문이 아니라 HWP 첨부파일에만 있는 경우**가 많았다. 웹에는 신청기간과 문의처만 있고, 자격·서류는 파일을 열어야 알 수 있다
- 결과적으로 **"읽다가 포기"** 가 발생하고, 자격이 되는데도 신청하지 않는 사람이 생긴다

### 1-2. 기존 방식과의 차이

| | 기존 | 되나요 |
|---|---|---|
| 정보 탐색 | 포털에서 정책 목록을 훑고 → 공고문 PDF를 열고 → 직접 읽음 | 이미 찾은 공고문을 **붙여넣기 1회** |
| 자격 판단 | 조건문을 스스로 해석 | **체크리스트로 분해**되어 하나씩 확인 |
| 서류 준비 | 본문·별첨에 흩어진 것을 직접 취합 | 목록으로 한 번에 |
| 결과 | 읽다가 포기 | 다음 행동(서류 준비 / 문의 / 포기)을 즉시 결정 |

> **"정책을 찾아주는 서비스"가 아니다.** 이미 찾은 공고문을 **읽어주는** 서비스다. 이 구분이 서비스 정체성의 핵심이다.

#### 유사 서비스 조사

정부지원사업 공고 통합 검색 서비스(govgrant)를 확인했다. 검색은 이미 있고, **찾은 다음 읽기**가 비어 있다.

| | 통합 검색 서비스 | 되나요 |
|---|---|---|
| 하는 일 | 여러 출처의 공고를 **모아서 검색** | 이미 찾은 공고를 **읽어서 분해** |
| 타겟 | 창업자·소상공인·프리랜서 | 지원 경험 없는 20대 청년 |
| 입력 | 검색어 | 공고문 텍스트 붙여넣기 |
| 출력 | 공고 목록 | 요약 + 자격 체크리스트 + 서류 + 기한 |
| 사용 시점 | 공고를 찾기 **전** | 공고를 찾은 **후** |

### 1-3. 목표

1. 공고문을 처음 본 사용자가 **1분 안에** 자기가 대상인지 판단할 수 있게 한다
2. 신청 포기의 원인이 "자격 미달"이지 "이해 실패"가 아니게 만든다
3. AI 출력이 원문을 왜곡하지 않도록, **공고문에 없는 내용은 생성하지 않는다**는 원칙을 기술적으로 강제한다

---

## 2. 타겟 사용자

### 2-1. 주 사용자

**정책 지원을 받아본 적 없는 20대 초·중반 (대학생 / 사회초년생 / 구직자)**

- 행정 문서를 읽어본 경험이 적어, 공고문 특유의 문체 자체가 진입 장벽이다
- 지원 이력이 없어 "소득분위", "가구원", "중위소득 150% 이하" 같은 용어의 감각이 없다
- 모바일에서 확인하는 비중이 높다 → **반응형이 선택이 아니라 필수**

### 2-2. 사용 상황

> 학교 게시판·카카오톡 단톡방·인스타그램에서 청년 지원 공고 링크를 받았다.
> PDF를 열어 두 문단 읽다가 "이거 나 되는 건가?" 싶어 스크롤을 내리다 창을 닫는다.
> — **바로 이 순간**, 그 텍스트를 복사해 되나요에 붙여넣는다.

### 2-3. 이 사용자가 얻는 것

- 대상 여부를 **판단할 수 있는 형태**로 받는다 (판단 자체를 대신해 주지 않는다 — §4-5 참고)
- 서류 목록을 미리 확보해 마감 직전 누락을 막는다
- 용어 풀이가 붙어 있어, 다음 공고문은 조금 더 수월하게 읽는다

### 2-4. 부차 사용자

- 청년 정책 담당 실무자 — 자기가 쓴 공고문이 어떻게 읽히는지 역으로 확인
- 대학 학생지원팀 / 취업지원관 — 학생에게 안내할 때 요약본으로 활용

---

## 3. 페이지·섹션 구성

단일 페이지(`index.html`) 안에 3개 섹션을 두고, 상단 고정 네비게이션에서 앵커로 이동한다.
(과제 요구: *"최소 3개 이상의 페이지 또는 섹션(메뉴 이동 가능)"*)

| # | 섹션 | 앵커 | 목적 | 주요 요소 |
|---|---|---|---|---|
| 1 | 홈 | `#home` | 3초 안에 무엇을 하는 서비스인지 전달 | 헤드라인 "그래서, 제가 되나요?" / 문제 제기 3줄 / CTA 버튼 → `#check` |
| 2 | **확인하기** | `#check` | 핵심 기능. AI 변환 수행 | 텍스트 입력(textarea) / 글자수 카운터 / [샘플 공고문 넣어보기] / [결과 확인하기] / 결과 4블록 / 안내 문구 영역 |
| 3 | 이용 안내 | `#guide` | 신뢰 형성 + 한계 고지 | 사용법 3단계 / **AI 요약의 한계 고지** / 자주 나오는 용어 풀이 / 공식 출처 링크 |

### 3-1. 네비게이션

- 데스크톱: 상단 고정 헤더에 `홈 / 확인하기 / 이용 안내` 3개 링크 + 로고
- 모바일: 햄버거 버튼 → 토글 메뉴
- 앵커 이동 시 `scroll-behavior: smooth`, 고정 헤더 높이만큼 `scroll-margin-top` 적용

### 3-2. 홈 → 확인하기 유도

홈 섹션 CTA는 단순 앵커 이동이 아니라, 이동 후 **입력창에 포커스**까지 준다. 사용자가 스크롤 후 다시 클릭해야 하는 단계를 없앤다.

---

## 4. AI 기능 설계

### 4-1. 기능 정의

| 항목 | 내용 |
|---|---|
| 기능명 | 공고문 해설 (공고문 → 쉬운 말 변환) |
| 엔드포인트 | `POST /api/generate` |
| AI 모델 | Google Gemini (`google-genai` SDK) |
| 호출 횟수 | 사용자 요청 1건당 **1회** |

### 4-2. 입력

| 항목 | 사양 |
|---|---|
| 형식 | 자유 텍스트 (공고문 본문 복사·붙여넣기) 또는 온통청년에서 고른 정책 필드 |
| UI | `<textarea>` + 온통청년 검색 목록. 고르면 입력창에 채워짐 |
| 필수 여부 | 필수 |
| 길이 제한 | **10자 이상 2,000자 초과 금지** |
| 전처리 | 앞뒤 공백 제거 |

> **길이 상한을 두는 이유** — ①Gemini 무료 티어 토큰 소비 억제 ②응답 지연 억제 ③Vercel 요청 본문 4.5MB 한도와 무관하게, 공고문 전체가 아니라 "지원 자격·서류·기한" 부분만 붙여넣도록 유도하는 UX 장치
> **하한(10자)을 두는 이유** — "ㅇㅇ", "테스트" 같은 무의미 입력에 AI 호출을 낭비하지 않기 위함

온통청년 연동은 **검색 서비스로 바꾸기 위한 것이 아니다.**  
군산 대시보드(`GunSan-youth-dashboard-KOSIS`)와 같은 `getPlcy` API로 전북·군산 정책을 고르게 한 뒤, 그 필드를 공고문 텍스트로 펼쳐 기존 `/api/generate`에 넣는다. 붙여넣기 경로는 그대로 둔다.

### 4-3. 출력

AI는 **JSON만** 반환하고, 프론트엔드가 이를 4개 블록으로 렌더링한다.

```json
{
  "is_policy": true,
  "title": "공고문에서 파악한 사업명",
  "summary": ["요약 1", "요약 2", "요약 3"],
  "eligibility": [
    { "item": "만 19~34세", "note": "신청일 기준" },
    { "item": "군산시 거주", "note": "주민등록 기준, 공고문에 기간 명시 없음" }
  ],
  "documents": ["신분증", "주민등록등본", "소득증빙 서류"],
  "deadline": "2026년 9월 30일 18:00까지",
  "terms": [
    { "word": "중위소득", "meaning": "전 국민을 소득순으로 줄 세웠을 때 정확히 가운데 있는 사람의 소득" }
  ]
}
```

**화면 렌더링 (4블록)**

| 블록 | 내용 | 비고 |
|---|---|---|
| ① 3줄 요약 | `summary` | 무슨 사업이고 얼마를 어떻게 주는지 |
| ② **내가 대상인지 체크리스트** | `eligibility` | 각 항목을 체크박스로 렌더링. 사용자가 직접 체크 |
| ③ 준비 서류 | `documents` | 목록 |
| ④ 신청 기한 | `deadline` + `terms` | 기한 강조 + 용어 풀이 |

**②를 체크박스로 만드는 이유** — AI가 "당신은 대상입니다"라고 **판정하지 않는다.** 판단 근거만 분해해 주고 최종 판단은 사용자가 한다. 정책·금융 도메인에서 AI가 자격을 단정하면 오판의 책임 소재가 생기고, 애초에 AI는 사용자의 나이·소득·거주지를 모른다. 이 설계는 §4-5의 한계 고지와 짝을 이룬다.

### 4-4. 환각(hallucination) 방지 — 이 도메인의 핵심 요구사항

정책 정보에서 없는 내용을 지어내면 사용자가 잘못된 서류를 준비하거나 마감을 놓친다. 다음을 프롬프트로 강제한다.

1. **원문에 없는 정보는 절대 생성하지 않는다**
2. 원문에 없으면 해당 필드에 `"공고문에 명시되지 않음"` 을 넣는다
3. 일반 상식이나 다른 정책 지식으로 빈칸을 메우지 않는다
4. 입력이 정책 공고문이 아니면 `is_policy: false` 로 반환하고 나머지는 비운다

### 4-5. 한계 고지 (UI 고정 문구)

결과 블록 하단에 **항상** 표시한다.

> ⚠️ 이 결과는 AI가 붙여넣은 텍스트만 보고 정리한 **참고 자료**입니다.
> 최종 자격과 서류는 반드시 **공고 원문과 담당 기관**을 통해 확인하세요.

### 4-6. 실패 처리 기준 ★ 과제 필수 항목

| # | 상황 | 판단 기준 | 사용자에게 보이는 문구 | HTTP | 처리 위치 |
|---|---|---|---|---|---|
| 1 | **빈 입력** | 공백 제거 후 길이 0 | "공고문 내용을 붙여넣어 주세요." | 400 | 클라이언트 + 서버 |
| 2 | 입력 과소 | 10자 미만 | "내용이 너무 짧습니다. 지원 자격이나 서류 부분을 함께 붙여넣어 주세요." | 400 | 클라이언트 + 서버 |
| 3 | 입력 과다 | 2,000자 초과 | "2,000자까지 입력할 수 있습니다. 지원 자격·서류·기한 부분만 붙여넣어 보세요." | 400 | 클라이언트 + 서버 |
| 4 | **API 오류** | AI 호출 중 예외 발생 | "결과를 가져오지 못했습니다. 잠시 후 다시 시도해 주세요." | 502 | 서버 |
| 5 | 응답 형식 오류 | AI 응답 JSON 파싱 실패 | "결과를 정리하지 못했습니다. 내용을 조금 줄여서 다시 시도해 주세요." | 502 | 서버 |
| 6 | 서버 설정 오류 | 환경변수 `GEMINI_API_KEY` 없음 | "서비스 점검 중입니다. 잠시 후 다시 시도해 주세요." | 500 | 서버 |
| 7 | **지연 / 타임아웃** | 클라이언트 응답 대기 25초 초과 | "응답이 지연되고 있습니다. 잠시 후 다시 시도해 주세요." | (클라 abort) | 클라이언트 |
| 8 | 네트워크 단절 | fetch 자체 실패 | "네트워크 연결을 확인해 주세요." | (클라) | 클라이언트 |
| 9 | 공고문 아님 | `is_policy: false` | "정책 공고문으로 보이지 않습니다. 지원 자격이나 신청 방법이 담긴 부분을 붙여넣어 주세요." | 200 | 클라이언트 |

> 과제 요구는 *"최소 1개 이상"* 이지만, 1·4·7번(빈 입력 / API 오류 / 지연)을 모두 구현한다. 세 가지가 각각 다른 계층(클라이언트 검증 / 서버 예외 / 클라이언트 abort)에서 발생하므로, 구현하면서 요청-응답 흐름 전체를 이해하게 된다. 과제 목표 2번과 직결된다.

**클라이언트와 서버에서 같은 검증을 두 번 하는 이유** — 클라이언트 검증은 UX용(즉시 피드백 + 불필요한 API 호출 차단)이고, 서버 검증은 보안용이다. `curl`로 직접 요청하면 클라이언트 검증은 그대로 우회된다.

### 4-7. UI 상태 정의

버튼 클릭 후 화면은 반드시 아래 4개 중 하나여야 한다.

| 상태 | 버튼 | 결과 영역 | 안내 영역 |
|---|---|---|---|
| `idle` | 활성 / "결과 확인하기" | 비어 있음 | 비어 있음 |
| `loading` | **비활성** / "정리하는 중..." | 로딩 표시 | 비어 있음 |
| `success` | 활성 복귀 | 4블록 렌더링 | 비어 있음 |
| `error` | 활성 복귀 | 비어 있음 | 안내 문구 |

> 로딩 중 버튼 비활성화는 **연타로 인한 무료 티어 쿼터 소진을 막는 장치**다.

---

## 5. 기술 스택 및 구조

### 5-1. 스택

| 구분 | 기술 | 선택 이유 |
|---|---|---|
| 프론트엔드 | HTML5 / CSS3 / Vanilla JavaScript | 과제 제약. 프레임워크 없이 DOM·이벤트·fetch를 직접 다루기 위함 |
| 백엔드 | Vercel Serverless Functions (Python, `BaseHTTPRequestHandler`) | 과제 제약. 상시 서버 없이 요청 시에만 실행 |
| AI | Google Gemini (`google-genai`) | 무료 티어로 학습 목적 호출 가능 |
| 배포 | Vercel (GitHub 연동 자동 배포) | push → 자동 재배포 |
| 형상 관리 | Git / GitHub | 커밋 이력으로 작업 과정 기록 |

### 5-2. 폴더 구조

```
doenayo/
├── index.html          # 3개 섹션 전체
├── css/style.css       # 스타일 (CSS 변수 기반)
├── js/app.js           # 입력 검증, fetch, 상태 전환, 결과 렌더링
├── api/generate.py     # 서버리스 함수 → /api/generate
├── images/             # 로고, OG 이미지
├── requirements.txt    # google-genai (단 한 줄)
├── .env.example        # 변수명만, 값 없음
├── .gitignore          # .env 포함
└── README.md
```

### 5-3. 동작 흐름

```
[브라우저]
  사용자가 공고문 붙여넣기 → [결과 확인하기] 클릭
       │
       │ ① 클라이언트 검증 (빈 값 / 10자 미만 / 2000자 초과)
       │ ② 상태 loading 전환, 버튼 비활성화
       │ ③ fetch('/api/generate', POST, JSON) + AbortController 25초
       ▼
[Vercel Serverless Function]  api/generate.py
       │ ④ 요청 본문 파싱 및 재검증
       │ ⑤ os.environ["GEMINI_API_KEY"] 확인    ← 키는 여기서만 존재
       │ ⑥ genai.Client().interactions.create(system_instruction=..., input=...)
       │ ⑦ 응답 JSON 파싱 → 실패 시 502
       ▼
[Gemini API]
       │
       ▼ (역방향)
[브라우저]
       ⑧ 상태 success/error 전환
       ⑨ 4블록 렌더링 또는 안내 문구 표시
```

### 5-4. 보안 설계 — API 키 관리

| 환경 | 저장 위치 | 커밋 여부 |
|---|---|---|
| 로컬 | 프로젝트 루트 `.env` | ❌ `.gitignore`로 차단 |
| 배포 | Vercel > Settings > Environment Variables (Production/Preview/Development 전부 체크) | ❌ 저장소에 없음 |

**프론트엔드에서 Gemini를 직접 호출하지 않는 이유**
프론트엔드 코드는 브라우저에 전부 내려간다. 키를 JS에 두면 개발자도구 소스 탭이나 Network 탭에서 그대로 노출되어 제3자가 내 쿼터를 소진시킬 수 있다. 서버리스 함수를 경유하면 키는 서버 환경에만 존재하고 브라우저는 `/api/generate`라는 **내 도메인 경로**만 알게 된다. 같은 오리진이므로 CORS 설정도 불필요하다.

### 5-5. 반응형 기준

| 구간 | 폭 | 레이아웃 |
|---|---|---|
| 모바일 | ~767px | 1단, 햄버거 메뉴, 결과 4블록 세로 나열 |
| 태블릿 | 768~1023px | 최대폭 720px 중앙 정렬, 상단 메뉴 노출 |
| 데스크톱 | 1024px~ | 최대폭 1080px, 입력·결과 2단 배치 검토 |

검증: Chrome DevTools **375px(iPhone SE)** / **1440px** + **실제 휴대폰으로 배포 URL 접속**

---

## 6. 범위 밖 (Out of Scope)

이번 버전에서 **의도적으로 제외**한 것과 그 이유.

| 제외 항목 | 이유 |
|---|---|
| 회원가입 / 로그인 | 개인정보 수집 없이도 핵심 가치가 성립. 인증은 이번 학습 목표가 아님 |
| 정책 검색·목록 제공 | "찾아주는 서비스"가 아니라 "읽어주는 서비스"라는 정체성 유지 |
| 온통청년 API 등 공공 API 연동 | 인증 발급·쿼터·응답 스키마 대응으로 일정 위험이 큼. 사용자 붙여넣기로 대체 |
| 결과 저장 / 히스토리 | 데이터베이스가 필요. 이번 범위의 서버리스 구조로는 과함 |
| PDF 파일 업로드 파싱 | 파싱 라이브러리 도입 시 번들 크기·처리 시간 증가 |
| 자격 여부 자동 판정 | **의도적 제외.** §4-3 참고 — AI가 자격을 단정하지 않는다 |

---

## 7. 성공 기준

| 구분 | 기준 |
|---|---|
| 기능 | 배포 URL에서 공고문 붙여넣기 → 4블록 결과 표시가 재현된다 |
| 기능 | 빈 입력 / API 오류 / 지연 상황에서 각각 다른 안내 문구가 표시된다 |
| 품질 | 375px과 1440px에서 레이아웃이 깨지지 않는다 |
| 보안 | 저장소·README·스크린샷 어디에도 API 키가 없다 |
| 내용 | AI 출력이 원문에 없는 정보를 만들어내지 않는다 (샘플 공고문 3건으로 확인) |
| 학습 | 과제 목표 6개 항목을 문서 없이 설명할 수 있다 |

---

## 8. 향후 확장 (보너스 과제 후보)

| 항목 | 내용 | 배움 포인트 |
|---|---|---|
| 다크 모드 | CSS 변수 기반 테마 전환 + `prefers-color-scheme` 초기값 | 사용자 OS 설정 존중, 접근성 |
| 방문자 분석 | Vercel Web Analytics | "개선 효과를 확인하는 방법" |
| 결과 공유 | 결과를 이미지·텍스트로 복사 | 사용자 입력 → 처리 → 활용 흐름 확장 |
