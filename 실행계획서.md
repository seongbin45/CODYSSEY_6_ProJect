# AI 웹 서비스 빌딩 — 실행 계획서

> 전제: Gemini 무료 티어 / GitHub·Vercel 계정 보유 / AI 코딩 도구 사용 중 / **며칠 내 마감**
> 문서 작성일 기준으로 Vercel·Google 공식 문서를 확인해 반영함 (근거는 마지막 "출처" 참고)

---

# PART 0. 먼저 알아야 할 것 — 이 과제의 실제 난이도는 어디에 있는가

## 0-1. 이 과제가 채점하는 것은 "웹사이트"가 아니다

과제 원문 2번은 결과물을 **제출 패키지 5종**으로 정의한다. 3번은 **"스스로 설명할 수 있어야 한다"** 는 6개 항목을 별도로 요구한다. 즉 채점 대상은 다음 두 축이다.

| 축 | 내용 | 실패 시 증상 |
|---|---|---|
| A. 산출물 완결성 | 배포 URL / GitHub / README / 기획서 / 증빙자료 — 5개 중 하나라도 빠지면 감점 | "사이트는 잘 만들었는데 기획서를 안 냄" |
| B. 설명 가능성 | HTML·CSS·JS 역할, fetch 흐름, Serverless 구조, 환경변수, 로컬↔배포 차이, 오류 원인 설명 | "AI가 다 짜줘서 왜 되는지 모름" |

**여기서 나오는 결론:** 코드 품질에 시간을 쓰는 것은 투자 대비 효율이 낮다. 시간은 (1) 배포가 실제로 되게 만드는 것, (2) 5종 패키지를 빠짐없이 채우는 것, (3) 만드는 과정을 기록해두는 것에 써야 한다.

특히 **증빙자료의 "AI 코딩 도구 사용 과정(대화 로그 또는 스크린샷)"** 은 나중에 만들 수 없다. 작업이 끝난 뒤 재현하려 하면 부자연스럽고 시간도 두 배로 든다. **1단계부터 기록을 시작해야 한다.**

## 0-2. 이 과제에 숨어 있는 기술적 함정 4개

원문에 안 적혀 있지만, 이 조합(정적 프론트 + `api/` 파이썬 + Vercel)에서 반드시 걸리는 지점들이다.

### 함정 ① `requirements.txt`에 Flask/FastAPI를 넣으면 사이트가 통째로 안 뜬다 — **최우선 경고**

Vercel의 현재 Python 런타임은 `requirements.txt`·`pyproject.toml`·`Pipfile`에서 **지원 프레임워크를 감지하면 "framework preset" 모드로 전환**된다. 공식 문서 표현 그대로:

> "A Python framework preset takes precedence over file-based functions. When Vercel detects a framework preset, the framework application handles all requests, and **files under `/api` don't become separate Vercel Functions.**"

즉 `requirements.txt`에 `flask`나 `fastapi` 한 줄이 들어가는 순간:
- `api/*.py` 파일 기반 함수가 **전부 무효화**되고
- 모든 요청이 그 프레임워크 앱으로 라우팅되어 **`index.html` 정적 프론트가 404** 가 된다

문제는 AI 코딩 도구에게 "파이썬 백엔드 만들어줘"라고 하면 **십중팔구 Flask를 제안한다**는 점이다. 이 함정 하나에서 하루를 날리는 경우가 가장 많다.

> **철칙: `requirements.txt`에는 `google-genai` 외에 아무것도 넣지 않는다.**

### 함정 ② `api/` 파일 기반 방식은 공식 문서상 "기존 프로젝트 지원(legacy)" 경로다

과제는 `api/` 폴더를 명시적으로 요구하지만, Vercel 문서는 이 방식을 *"Vercel supports **existing projects** that define file-based Python functions"* 로 서술하며 신규 프로젝트에는 FastAPI/Flask 프리셋을 권한다.

**영향:** 지금 웹에서 검색되는 "Vercel Python" 최신 튜토리얼 대부분이 과제 요구 구조와 **다른 구조**를 알려준다. AI 코딩 도구도 마찬가지다.

**대응:** 참조할 문서 페이지를 처음부터 하나로 고정한다 → `vercel.com/docs/functions/runtimes/python/api-directory`. 이 페이지 밖의 예제는 채택하지 않는다.

### 함정 ③ AI 코딩 도구가 **구버전 Gemini SDK 코드**를 뱉는다

Gemini 파이썬 SDK는 세대가 여러 번 바뀌었고, 학습 데이터에 남아 있는 옛 코드가 훨씬 많다. AI가 아래를 쓰면 **전부 틀렸다고 보고 교정**해야 한다.

| ❌ 낡은 코드 (AI가 자주 생성) | ✅ 현행 |
|---|---|
| `pip install google-generativeai` | `pip install google-genai` |
| `import google.generativeai as genai` | `from google import genai` |
| `genai.configure(api_key=...)` | `client = genai.Client()` (환경변수 `GEMINI_API_KEY` 자동 인식) |
| `genai.GenerativeModel("gemini-pro")` | 모델 문자열을 `client.interactions.create()`에 직접 전달 |
| `model.generate_content(...)` | `client.interactions.create(model=..., input=...)` — 공식 문서: *"The Interactions API is now generally available. We recommend using this API"* |
| `response.text` | `interaction.output_text` |

모델명은 시점에 따라 바뀐다. 작업 시작 전 **AI Studio에서 현재 사용 가능한 모델명을 직접 확인**하고 그 문자열로 고정할 것. (문서 확인 시점 기준 예: `gemini-3.6-flash`)

### 함정 ④ (역함정) "Vercel 10초 타임아웃"은 이제 **틀린 정보**다

블로그·AI 답변에서 아직도 "Hobby는 10초라 AI 호출이 타임아웃된다"고 하는데, 현재 공식 한도는 다음과 같다.

| 항목 | Hobby 플랜 현재 값 |
|---|---|
| 최대 실행 시간 | **300초** (기본값이자 최대값) |
| 메모리 | 2 GB / 1 vCPU |
| 요청·응답 본문 최대 | 4.5 MB |
| Python 번들 크기 | 500 MB |

**결론:** 서버 타임아웃 대응 코드를 짜느라 시간 쓸 필요 없다. 다만 과제가 요구하는 **"지연/타임아웃 안내"는 프론트엔드에서 `AbortController`로 직접 만들어야 한다.** (서버가 안 끊으니 내가 끊는다)

## 0-3. 전략적 판단 — 순서를 뒤집는다 (Walking Skeleton)

대부분의 사람은 이 순서로 간다:

> 기획 → 예쁜 3페이지 만들기 → AI 기능 붙이기 → 배포 → **배포에서 터짐** → 남은 시간 없음

배포는 이 과제에서 **가장 불확실성이 큰 구간**인데 맨 뒤에 배치되어 있다. 마감이 며칠이면 이건 자살행위다. 순서를 뒤집는다.

> **Day 1에 "못생긴 HTML 한 장 + api 한 개"로 배포부터 성공시킨다.**
> 브라우저 → `/api/generate` → Gemini → 화면 표시가 **실제 Vercel URL에서** 도는 것을 먼저 확인한다.

이걸 확보하면 남은 작업(페이지 3개, 스타일, 반응형)은 전부 **위험도 0의 장식 작업**이 된다. 실패해도 되돌리기 쉽고, 최악의 경우 그 상태로도 제출 최소 요건을 넘긴다.

**이 원칙은 협상 대상이 아니다. Day 1에 배포 성공을 못 하면 계획 전체를 재조정해야 한다.**

## 0-4. 전체 구조 한눈에 보기

```
[브라우저]
  index.html ── nav ──> #home / #ai / #about  (같은 문서 내 섹션 이동)
       │
       │  사용자 입력 → js/app.js
       │  fetch('/api/generate', {method:'POST', body:{input:"..."}})
       │  + AbortController 20초 제한
       ▼
[Vercel Serverless Function]  api/generate.py
       │  class handler(BaseHTTPRequestHandler) → do_POST
       │  1. 입력 검증 (빈 값 → 400)
       │  2. os.environ["GEMINI_API_KEY"]  ← 코드에 키 없음
       │  3. genai.Client().interactions.create(...)
       │  4. JSON 응답 (성공 200 / 검증실패 400 / AI오류 502)
       ▼
[Google Gemini API]
```

**환경변수 흐름 (과제 목표 4번의 답이 여기 있다)**

```
로컬:   .env  ──> vercel dev 가 읽음 ──> os.environ
                   ↑ .gitignore 로 절대 커밋 안 됨
배포:   Vercel 대시보드 > Settings > Environment Variables
                              ──> 빌드/런타임에 주입 ──> os.environ
```

키가 **브라우저로 내려가지 않는다**는 것이 핵심이다. 프론트에서 직접 Gemini를 부르면 개발자도구 Network 탭에서 키가 그대로 보인다. 서버리스 함수를 거치는 이유가 바로 이것이며, 이걸 말로 설명할 수 있으면 과제 목표 3·4번이 동시에 해결된다.

---

# PART 1. 미시 실행 계획 — 단계별

각 단계마다 **완료 판정 기준(Done)** 을 뒀다. Done을 통과하지 못하면 다음 단계로 넘어가지 않는다.

## 총괄 일정 (4일 / 약 20~24시간)

| Day | Phase | 시간 | 산출물 |
|---|---|---|---|
| 1 | Phase 0 사전 점검 | 0.5h | 키 발급, 계정 확인 |
| 1 | **Phase 1 워킹 스켈레톤 (최우선)** | 3h | **배포된 URL에서 AI 왕복 성공** |
| 1 | Phase 2 기획 확정 | 1.5h | 기획서 초안 |
| 2 | Phase 3 프론트 3섹션 + 네비 | 3h | 화면 완성 |
| 2 | Phase 4 AI 기능 UX + 실패 처리 | 2.5h | 3가지 실패 안내 동작 |
| 3 | Phase 5 반응형 | 2h | 2개 이상 화면폭 검증 |
| 3 | Phase 6 배포 검증 + 증빙 수집 | 2.5h | 스크린샷 세트, 로그 |
| 4 | Phase 7 README + 제출 패키지 | 2h | 5종 완비 |
| 4 | Phase 9 설명 가능성 자가점검 | 1h | 6문항 자문자답 |
| (선택) | Phase 8 보너스 | 2h | 다크모드 등 |

---

## Phase 0 — 사전 점검 (30분)

**목적:** 뒤에서 터질 것을 앞에서 터뜨린다.

### 0-1. Gemini API 키 발급
1. `aistudio.google.com/apikey` 접속 → 키 생성
2. 키를 안전한 곳에 임시 보관 (메모장, 절대 코드 아님)
3. **`aistudio.google.com/rate-limit` 에서 본인 계정의 실제 한도를 직접 확인**한다

> ⚠️ Gemini 무료 한도는 블로그마다 숫자가 다르고 대부분 낡았다. 공식 rate-limits 문서조차 *"Rate limits ... can be viewed in Google AI Studio"* 라며 대시보드로 안내한다. **본인 대시보드 숫자만 믿을 것.**

### 0-2. 개발 도구 설치
```bash
# Vercel CLI — 로컬에서 api/ 를 배포와 동일하게 돌리는 유일한 방법
npm i -g vercel
vercel --version
vercel login
```

> `python -m http.server` 로는 `api/`가 안 돈다. `vercel dev` 없이 하려면 매번 배포해서 테스트해야 하는데, 며칠 마감에서는 그 왕복 비용이 크다. **`vercel dev`를 쓴다.**

### 0-3. AI 코딩 도구용 제약 프롬프트 준비
아래를 텍스트 파일로 저장해두고, **AI 코딩 도구와 새 대화를 시작할 때마다 맨 앞에 붙여넣는다.** 함정 ①③을 사전에 차단하는 장치다.

```
[프로젝트 제약 — 반드시 준수]
1. 프론트엔드는 순수 HTML/CSS/JavaScript만 사용한다.
   React, Vue, Svelte, Tailwind CDN, 빌드 도구 일절 금지.
2. 백엔드는 Vercel Serverless Functions(Python)이며 api/ 폴더의 파일 기반 함수다.
   각 .py 파일은 반드시 http.server.BaseHTTPRequestHandler 를 상속한
   class handler 를 최상위에 정의한다.
3. requirements.txt 에는 google-genai 만 넣는다.
   Flask, FastAPI, Django, uvicorn 을 절대 추가하지 마라.
   추가하면 Vercel이 framework preset으로 전환되어 api/ 함수가 전부 무효화되고
   정적 index.html 이 404가 된다.
4. Gemini SDK는 신형 API만 쓴다.
   - pip: google-genai   (google-generativeai 아님)
   - import: from google import genai
   - client = genai.Client()      (genai.configure() 아님)
   - client.interactions.create(model=..., input=...)  
     (GenerativeModel / generate_content 아님)
   - 결과: interaction.output_text   (response.text 아님)
5. API 키는 os.environ["GEMINI_API_KEY"] 로만 읽는다.
   코드·주석·예제 어디에도 키 문자열을 넣지 마라.
6. 코드를 줄 때는 파일 경로를 명시하고, 왜 그렇게 짰는지 2줄로 설명하라.
```

**✅ Phase 0 Done:** 키 발급 완료 / `vercel login` 성공 / 제약 프롬프트 파일 준비됨

---

## Phase 1 — 워킹 스켈레톤 (3시간) 🔥 **이 과제의 승부처**

### Step 1-1. 저장소 생성과 `.gitignore` — **순서가 생명이다**

과제 제약사항에 *"키 유출이 의심되면 즉시 폐기/재발급하고, 노출된 커밋 이력도 정리할 수 있어야 한다"* 가 명시돼 있다. 커밋 이력에서 키를 지우는 건 `git rm --cached` 로 안 된다 (히스토리에 남는다). 애초에 안 들어가게 하는 게 유일한 해법이다.

```bash
mkdir my-ai-service && cd my-ai-service
git init

# ⚠️ 첫 번째 커밋은 반드시 .gitignore 여야 한다
cat > .gitignore <<'EOF'
.env
.env.local
.venv/
venv/
__pycache__/
*.pyc
.vercel/
.DS_Store
EOF

git add .gitignore
git commit -m "chore: add gitignore before anything else"
```

그 다음 폴더 골격:
```bash
mkdir -p css js api images
touch index.html css/style.css js/app.js api/generate.py requirements.txt README.md
echo "google-genai" > requirements.txt
```

최종 구조 (과제 4번 "프로젝트 초기화 및 구조 구성" 요구 충족):
```
my-ai-service/
├── index.html
├── css/style.css
├── js/app.js
├── api/generate.py
├── images/
├── requirements.txt
├── README.md
├── .gitignore
└── .env          ← 로컬 전용, 커밋 안 됨
```

`.env` 작성:
```
GEMINI_API_KEY=발급받은_키
```

**✅ Done:** `git status` 에 `.env` 가 **나타나지 않는다**. (나타나면 여기서 멈추고 해결)

### Step 1-2. 최소 백엔드 — `api/generate.py`

지금은 못생겨도 된다. **도는 것**만 확인한다.

```python
# api/generate.py
from http.server import BaseHTTPRequestHandler
import json
import os

from google import genai

MODEL = "gemini-3.6-flash"      # ← AI Studio에서 확인한 현재 모델명으로 교체
MAX_INPUT_LEN = 500

SYSTEM_PROMPT = """(여기에 서비스 성격에 맞는 지시문을 넣는다.
예: 너는 ○○ 도우미다. 한국어로, 3문장 이내로, 친근하게 답한다.)"""


class handler(BaseHTTPRequestHandler):

    def _send(self, status, payload):
        body = json.dumps(payload, ensure_ascii=False).encode("utf-8")
        self.send_response(status)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def do_POST(self):
        # 1) 요청 파싱
        try:
            length = int(self.headers.get("Content-Length") or 0)
            raw = self.rfile.read(length) if length else b"{}"
            data = json.loads(raw.decode("utf-8") or "{}")
        except (ValueError, UnicodeDecodeError):
            self._send(400, {"error": "요청 형식이 올바르지 않습니다."})
            return

        # 2) 입력 검증  → 과제 요구 "빈 입력" 실패 처리
        user_text = (data.get("input") or "").strip()
        if not user_text:
            self._send(400, {"error": "필수값을 입력하세요."})
            return
        if len(user_text) > MAX_INPUT_LEN:
            self._send(400, {"error": f"입력은 {MAX_INPUT_LEN}자 이하로 입력해 주세요."})
            return

        # 3) 환경변수 확인  → 배포 시 키 누락을 조기 발견
        if not os.environ.get("GEMINI_API_KEY"):
            print("CONFIG_ERROR: GEMINI_API_KEY is missing")
            self._send(500, {"error": "서버 설정 오류입니다. 잠시 후 다시 시도해 주세요."})
            return

        # 4) AI 호출  → 과제 요구 "API 오류(4xx/5xx)" 실패 처리
        try:
            client = genai.Client()          # GEMINI_API_KEY 를 자동으로 읽음
            interaction = client.interactions.create(
                model=MODEL,
                system_instruction=SYSTEM_PROMPT,
                input=user_text,
            )
            self._send(200, {"result": interaction.output_text})
        except Exception as e:
            # 상세 오류는 서버 로그에만. 사용자에게 내부 정보를 노출하지 않는다.
            print("GEMINI_ERROR:", repr(e))
            self._send(502, {"error": "AI 응답을 가져오지 못했습니다. 잠시 후 다시 시도해 주세요."})
```

**설계 의도 (README·구술 방어에 그대로 쓸 수 있다):**
- `class handler(BaseHTTPRequestHandler)` — Vercel 파일 기반 Python 함수가 인식하는 3가지 최상위 이름(`app`/`application`/`handler`) 중 프레임워크 없이 쓸 수 있는 유일한 형태
- 파일 경로가 곧 라우트 → `api/generate.py` = `/api/generate`
- 상태 코드를 400/500/502로 나눈 이유 → 프론트가 상황별로 다른 안내 문구를 띄울 수 있게 하기 위함
- 예외 원문을 사용자에게 안 보내는 이유 → 키·내부 경로 유출 방지

### Step 1-3. 최소 프론트 — `index.html`

```html
<!DOCTYPE html>
<html lang="ko">
<head><meta charset="UTF-8"><title>skeleton</title></head>
<body>
  <textarea id="q" rows="3"></textarea>
  <button id="go">보내기</button>
  <p id="msg"></p>
  <pre id="out"></pre>

  <script>
    document.getElementById('go').onclick = async () => {
      const msg = document.getElementById('msg');
      const out = document.getElementById('out');
      msg.textContent = '요청 중...'; out.textContent = '';
      try {
        const res = await fetch('/api/generate', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ input: document.getElementById('q').value })
        });
        const data = await res.json();
        if (!res.ok) { msg.textContent = data.error; return; }
        msg.textContent = ''; out.textContent = data.result;
      } catch (e) { msg.textContent = '네트워크 오류가 발생했습니다.'; }
    };
  </script>
</body>
</html>
```

### Step 1-4. 로컬 검증

```bash
vercel dev
# http://localhost:3000 접속 → 입력 후 [보내기]
```

- 결과가 나오면 통과
- 빈 값으로 눌러 `"필수값을 입력하세요."` 가 뜨는지도 확인

### Step 1-5. GitHub 푸시

```bash
git add .
git commit -m "feat: walking skeleton - static page calls python api and gemini"
git branch -M main
git remote add origin https://github.com/<본인>/<저장소>.git
git push -u origin main
```

**커밋 이력 남기기(과제 요구)** — 마지막에 한 번에 몰아 커밋하지 말고, Phase마다 1~3개씩 의미 단위로 쪼갠다. `feat:` / `fix:` / `docs:` / `style:` 접두어를 쓰면 이력만 봐도 과정이 드러난다.

### Step 1-6. Vercel 배포 ⚠️ **설정 하나만 틀려도 안 된다**

1. `vercel.com/new` → GitHub 저장소 Import
2. **Framework Preset = `Other`** ← Next.js 등으로 잡히면 반드시 바꾼다
3. Root Directory = `./` (그대로)
4. Build/Output 설정은 건드리지 않는다 (정적 파일이므로 빌드 불필요)
5. **Environment Variables** 에 추가:
   - Name: `GEMINI_API_KEY`
   - Value: 발급받은 키
   - **Environment: Production / Preview / Development 를 모두 체크** ← 여기를 놓쳐서 "로컬은 되는데 배포는 500" 이 나는 경우가 가장 흔하다
6. Deploy

### Step 1-7. 배포 URL에서 검증 (**localhost 아님**)

- 배포 URL 접속 → AI 기능 정상 동작
- 500이 뜨면 → Vercel 대시보드 > Deployments > (해당 배포) > **Runtime Logs** 에서 `CONFIG_ERROR` 나 `GEMINI_ERROR` 확인

> 환경변수를 나중에 추가/수정하면 **재배포해야 반영된다.** Deployments 탭에서 Redeploy.

**✅ Phase 1 Done — 아래 3개가 전부 참이어야 한다**
1. 공개 URL에서 입력 → AI 결과 표시가 된다
2. 빈 입력 시 안내 문구가 뜬다
3. GitHub 저장소 어디에도 API 키 문자열이 없다 (`git log -p | grep -i "AIza"` 로 확인)

**여기까지 오면 과제의 위험 구간은 끝났다.** 남은 건 전부 되돌리기 쉬운 작업이다.

---

## Phase 2 — 서비스 기획 확정 (1.5시간)

기획서는 제출 필수 5종 중 하나다. 스켈레톤이 도는 걸 보고 나서 쓰면 "구현 가능한 범위"를 알기 때문에 훨씬 현실적으로 써진다. 그래서 Phase 1 뒤에 배치했다.

### 기획 시 반드시 통과해야 할 자기검열 3개

과제 제약: *"템플릿/예시를 그대로 복제하지 않는다(아이디어·문구·구성 변경 필수)"*

1. **원문 예시("멍냥케어" 반려동물 추천)와 구조가 겹치지 않는가?** 타겟만 바꾸고 "입력 3개 → 추천 문구 출력" 구조를 그대로 쓰면 복제로 읽힌다. **AI 기능의 형태 자체**를 다르게 가져가라 (예: 추천이 아니라 변환·요약·진단·비교·생성).
2. **AI 기능의 출력이 "그럴듯한 문장"에서 끝나지 않는가?** 사용자가 그 출력을 받고 실제로 뭘 할 수 있는지 한 문장으로 답할 수 없으면 기획이 약한 것이다.
3. **하루 안에 만들 수 있는가?** AI 기능은 **1개면 충분하다.** 2개 이상 만들려는 유혹은 마감 리스크만 키운다.

### 기획서 목차 (과제 요구 항목을 그대로 매핑)

```markdown
# [서비스명] 서비스 기획서

## 1. 서비스 개요
- 서비스명 / 한 줄 소개
- 해결하려는 문제 (기존에는 이걸 어떻게 하고 있었는가)
- 목적

## 2. 타겟 사용자
- 주 사용자 / 사용 상황(언제, 어디서, 왜 켜는가)
- 이 사용자가 얻는 구체적 이득

## 3. 페이지·섹션 구성 (최소 3개)
| # | 섹션 | 목적 | 주요 요소 |
|---|------|------|----------|
| 1 | 홈(Hero) | 3초 안에 뭐 하는 서비스인지 전달 | 헤드라인, CTA |
| 2 | AI 기능 | 핵심 가치 제공 | 입력 폼, 결과 영역 |
| 3 | 소개/FAQ | 신뢰 형성, 사용법 안내 | 설명, 문답 |
- 이동 방식: 상단 고정 네비 + 앵커 이동(#) / 모바일은 햄버거 메뉴

## 4. AI 기능 설계        ← 과제가 명시적으로 요구하는 항목
### 4-1. 입력
- 항목, 형식, 필수 여부, 길이 제한(500자)
### 4-2. 출력
- 형태(문단/목록/표), 분량, 톤
### 4-3. 사용자에게 제공하는 가치
### 4-4. 실패 처리 기준       ← 여기가 채점 포인트
| 상황 | 판단 기준 | 사용자에게 보이는 문구 | HTTP 상태 |
|------|----------|---------------------|----------|
| 빈 입력 | 공백 제거 후 길이 0 | "필수값을 입력하세요." | 400 |
| 입력 초과 | 500자 초과 | "입력은 500자 이하로 입력해 주세요." | 400 |
| API 오류 | AI 호출 예외 발생 | "AI 응답을 가져오지 못했습니다. 잠시 후 다시 시도해 주세요." | 502 |
| 지연/타임아웃 | 클라이언트 20초 초과 | "응답이 지연되고 있습니다. 잠시 후 다시 시도해 주세요." | (클라 abort) |
| 설정 오류 | 환경변수 없음 | "서버 설정 오류입니다." | 500 |

## 5. 기술 스택 및 구조
- (PART 0-4의 구조 다이어그램을 여기에 넣는다)

## 6. 범위 밖(Out of scope)
- 이번에 하지 않기로 한 것 + 그 이유
```

> §6은 요구사항엔 없지만 넣기를 권한다. "회원가입은 이번 범위에서 제외했다"고 명시하면 기능 누락이 아니라 **의도된 판단**으로 읽힌다.

**✅ Phase 2 Done:** 기획서에 §4-4 실패 처리 표가 채워졌고, 그 표가 Phase 1 코드의 실제 동작과 일치한다

---

## Phase 3 — 프론트엔드 화면 구현 (3시간)

### 3-1. 구성 방식 결정 — 단일 페이지 앵커 방식을 권한다

| 방식 | 장점 | 단점 |
|---|---|---|
| **① 한 파일 + 섹션 앵커 (`index.html#ai`)** | 파일 1개, JS 단순, 배포 라우팅 이슈 0 | 파일이 길어짐 |
| ② 파일 분리 (`index.html`, `about.html`, `ai.html`) | 구조가 명확 | 네비를 파일마다 복붙 → 수정 시 누락 발생 |

과제는 *"최소 3개 이상의 페이지 **또는 섹션**"* 을 허용한다. 며칠 마감이면 **①**이 정답이다. 유지보수 실수 지점이 압도적으로 적다.

### 3-2. 시맨틱 마크업으로 짜라 (설명 가능성 확보용)

```html
<header>  <nav> 로고 + 메뉴 3개 + 햄버거 버튼 </nav> </header>
<main>
  <section id="home">  Hero  </section>
  <section id="ai">    AI 기능 (입력 폼 + 결과)  </section>
  <section id="about"> 소개 / FAQ  </section>
</main>
<footer>  </footer>
```

`<div>` 로만 도배된 코드를 받으면 과제 목표 1번("HTML/CSS/JS 역할 설명")을 답하기 어려워진다. AI에게 **"시맨틱 태그로 작성하라"** 를 명시적으로 요구할 것.

### 3-3. 네비게이션
```css
html { scroll-behavior: smooth; }
header { position: sticky; top: 0; z-index: 10; }
section { scroll-margin-top: 64px; }  /* 고정 헤더에 제목이 가려지는 것 방지 */
```
> `scroll-margin-top` 누락이 흔한 버그다. 앵커 이동 시 섹션 제목이 헤더 뒤로 숨는다.

### 3-4. CSS 기본기
- CSS 변수로 색/간격 토큰화 (`:root { --c-bg: ...; --sp-4: ... }`) → 나중에 다크모드 보너스가 15분 작업이 된다
- 레이아웃은 Flexbox / Grid
- 폰트는 시스템 폰트 스택 (외부 폰트 로딩은 모바일 속도만 깎는다)

**✅ Phase 3 Done:** 3개 섹션 존재 / 네비 클릭 시 정확한 위치로 이동 / 헤더에 제목 안 가림

---

## Phase 4 — AI 기능 UX 완성 (2.5시간)

### 4-1. 상태 기계로 설계한다

버튼을 눌렀을 때 화면은 반드시 다음 4개 중 하나여야 한다. 이 4개를 명시적으로 다루지 않으면 "결과가 안 나오는데 아무 반응 없음" 상태가 생긴다.

```
idle ──클릭──> loading ──> success   (결과 표시)
                    └────> error     (안내 문구 표시)
```

| 상태 | 버튼 | 결과 영역 | 안내 영역 |
|---|---|---|---|
| idle | 활성 | 비어있음 | 비어있음 |
| loading | **비활성 + "생성 중..."** | 스켈레톤/스피너 | 비어있음 |
| success | 활성 복귀 | 결과 텍스트 | 비어있음 |
| error | 활성 복귀 | 비어있음 | 빨간 안내 문구 |

> **버튼 비활성화는 필수다.** 안 하면 사용자가 연타하고, 무료 티어 쿼터가 순식간에 마른다.

### 4-2. `js/app.js` — 타임아웃 포함 완성형

```js
const TIMEOUT_MS = 20000;

async function askAI(text) {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), TIMEOUT_MS);

  try {
    const res = await fetch('/api/generate', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ input: text }),
      signal: controller.signal,
    });

    const data = await res.json().catch(() => ({}));

    if (!res.ok) {
      // 서버가 내려준 안내 문구를 그대로 사용 (400/500/502)
      throw new Error(data.error || '요청을 처리하지 못했습니다.');
    }
    return data.result;

  } catch (err) {
    if (err.name === 'AbortError') {
      throw new Error('응답이 지연되고 있습니다. 잠시 후 다시 시도해 주세요.');
    }
    if (err instanceof TypeError) {
      throw new Error('네트워크 연결을 확인해 주세요.');
    }
    throw err;
  } finally {
    clearTimeout(timer);
  }
}

const form   = document.getElementById('ai-form');
const input  = document.getElementById('ai-input');
const btn    = document.getElementById('ai-submit');
const result = document.getElementById('ai-result');
const notice = document.getElementById('ai-notice');

form.addEventListener('submit', async (e) => {
  e.preventDefault();

  // 클라이언트 1차 검증 — 서버 왕복 없이 즉시 안내 (쿼터 절약)
  const text = input.value.trim();
  if (!text) {
    notice.textContent = '필수값을 입력하세요.';
    input.focus();
    return;
  }

  // loading
  btn.disabled = true;
  btn.textContent = '생성 중...';
  notice.textContent = '';
  result.textContent = '';

  try {
    result.textContent = await askAI(text);   // success
  } catch (err) {
    notice.textContent = err.message;         // error
  } finally {
    btn.disabled = false;
    btn.textContent = '결과 받기';
  }
});
```

> **서버 검증을 없애면 안 된다.** 클라이언트 검증은 UX용이고, 서버 검증은 보안용이다. `curl` 로 빈 요청을 보내면 클라 검증은 그냥 우회된다. 둘 다 있어야 한다 — 이건 구술 방어 때 좋은 답변 소재다.

### 4-3. 접근성 (비용 대비 효과가 큼)
```html
<div id="ai-notice" role="alert" aria-live="assertive"></div>
<div id="ai-result" aria-live="polite"></div>
```
2줄로 스크린리더 대응이 되고, "왜 넣었나"를 설명할 수 있으면 가산점 요소다.

### 4-4. 검증 테스트 (과제 원문 8번 테스트 케이스와 대응)

| # | 입력 | 기대 결과 | 확인 |
|---|---|---|---|
| 1 | 정상 입력 | 결과 표시 | ☐ |
| 2 | 빈 값 | "필수값을 입력하세요." | ☐ |
| 3 | 200자 이상 긴 입력 | 결과 표시 (또는 지연 안내) | ☐ |
| 4 | 501자 입력 | 길이 초과 안내 | ☐ |
| 5 | 네트워크 끊고 전송 (DevTools > Network > Offline) | 네트워크 오류 안내 | ☐ |
| 6 | `TIMEOUT_MS`를 임시로 100으로 바꾸고 전송 | 지연 안내 문구 | ☐ |
| 7 | Vercel 환경변수를 임시 삭제 후 전송 | 서버 설정 오류 안내 | ☐ |

> #5·#6·#7은 **증빙 스크린샷을 찍기 위한 인위적 재현**이다. 찍고 나서 원래대로 되돌리는 걸 잊지 말 것.

**✅ Phase 4 Done:** 위 표 7개 전부 체크 + 각 상황 스크린샷 확보

---

## Phase 5 — 반응형 (2시간)

과제 요구: *"최소 2가지 화면 크기에서 직접 확인한다"*

### 5-1. 필수 선행 조건
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```
> 이 한 줄이 없으면 CSS 미디어쿼리를 아무리 써도 모바일에서 안 먹는다. 반응형 문제의 최다 원인.

### 5-2. 모바일 퍼스트 접근
```css
/* 기본 = 모바일 */
.container { padding: 16px; }
.nav-links { display: none; }        /* 햄버거로 토글 */

@media (min-width: 768px) {          /* 태블릿 */
  .container { max-width: 720px; margin: 0 auto; }
  .nav-links { display: flex; }
  .hamburger { display: none; }
}

@media (min-width: 1024px) {         /* 데스크톱 */
  .container { max-width: 1080px; }
}
```

### 5-3. 실제 검증 절차
1. Chrome DevTools > 기기 툴바(Ctrl+Shift+M)
2. **iPhone SE (375px)** 에서 확인 — 가장 좁은 실사용 폭. 여기서 안 깨지면 대부분 안전
3. **iPad (768px)** 확인
4. **데스크톱 (1440px)** 확인
5. **실제 휴대폰으로 배포 URL 접속** ← DevTools 에뮬레이션이 못 잡는 것들이 있다

### 5-4. 모바일 체크리스트
- ☐ 가로 스크롤바가 생기지 않는가 (넘치는 요소 범인 찾기: 긴 URL, 고정 width, 큰 이미지)
- ☐ 버튼·링크가 손가락으로 눌리는 크기인가 (최소 44×44px)
- ☐ 입력창 포커스 시 화면이 확대되지 않는가 (입력 요소 `font-size: 16px` 이상이면 방지됨)
- ☐ AI 결과가 긴 문장일 때 넘치지 않는가 (`word-break`, `overflow-wrap: anywhere`)
- ☐ 햄버거 메뉴가 열리고 닫히는가

**✅ Phase 5 Done:** 375px / 1440px 두 폭에서 깨짐 없음 + 실제 폰에서 확인

---

## Phase 6 — 배포 재검증 및 증빙 수집 (2.5시간)

### 6-1. 최종 배포
```bash
git add .
git commit -m "feat: complete responsive layout and ai ux"
git push
```
Vercel이 자동 배포 → **Deployment 상태가 Ready 인지 확인** (Building 중에 테스트하면 옛 버전을 본다)

### 6-2. 배포 URL 전수 검증 (localhost 금지)
- ☐ 3개 섹션 네비 이동
- ☐ AI 기능 정상 동작
- ☐ 실패 처리 안내 동작
- ☐ 모바일 표시
- ☐ 시크릿 창에서 접속 (캐시·로그인 상태 영향 배제)
- ☐ **다른 기기/다른 사람에게 URL 보내서 접속 확인** ← 과제 제약: *"동료가 접속/테스트 가능"*

### 6-3. 증빙 자료 수집 — 폴더로 정리

```
증빙자료/
├── 01_스크린샷/
│   ├── desktop_home.png
│   ├── desktop_ai_섹션.png
│   ├── mobile_home.png          (375px)
│   ├── mobile_ai_섹션.png
│   ├── ai_동작_입력.png          ← 입력이 채워진 상태
│   ├── ai_동작_결과.png          ← 결과가 표시된 상태
│   ├── 실패_빈입력.png
│   ├── 실패_지연안내.png
│   └── 실패_api오류.png
└── 02_AI코딩도구_사용과정/
    ├── 01_초기구조_생성.png
    ├── 02_flask제안_교정.png     ← 함정①을 잡아낸 대화. 가장 값어치 있는 증빙
    ├── 03_구버전SDK_교정.png     ← 함정③
    ├── 04_오류_디버깅.png
    └── conversation_log.md      ← 대화 원문 텍스트
```

**스크린샷 3원칙**
1. **배포 URL이 주소창에 보이게** 찍는다 (localhost:3000이 찍혀 있으면 배포 증빙이 안 된다)
2. **API 키가 화면 어디에도 없어야 한다** — DevTools, 터미널, 환경변수 설정 화면 캡처 시 특히 주의. Vercel 환경변수 화면을 찍는다면 값은 가려져 있어야 한다
3. 브라우저 확대율 100%

**AI 대화 로그의 핵심**
단순히 "코드 짜줘 → 짜줌" 로그는 증빙 가치가 낮다. **AI가 틀린 것을 내가 잡아낸 대화**가 과제 목표 6번(*"오류 원인을 파악하고 수정 방향을 말로 설명"*)의 직접 증거다. 함정 ①③을 교정한 대화를 반드시 남겨라. 이게 이 증빙 세트에서 가장 값어치 있는 부분이다.

**✅ Phase 6 Done:** 스크린샷 세트 완비 + 제3자 접속 확인 + 키 노출 0

---

## Phase 7 — README 및 제출 패키지 (2시간)

### 7-1. README.md 템플릿

```markdown
# [서비스명]

> 한 줄 소개

🔗 **배포 URL:** https://xxx.vercel.app

![스크린샷](images/screenshot.png)

## 1. 서비스 소개
- 해결하는 문제 / 타겟 사용자 / 핵심 가치

## 2. 주요 기능
| 섹션 | 기능 |
|---|---|
| 홈 | ... |
| AI ○○ | 입력 → AI 처리 → 결과 표시 |
| 소개 | ... |

## 3. 기술 스택
| 구분 | 기술 |
|---|---|
| 프론트엔드 | HTML5, CSS3, Vanilla JavaScript (프레임워크 미사용) |
| 백엔드 | Vercel Serverless Functions (Python 3.12, BaseHTTPRequestHandler) |
| AI | Google Gemini API (`google-genai`) |
| 배포 | Vercel (GitHub 연동 자동 배포) |
| 버전 관리 | Git / GitHub |

## 4. 프로젝트 구조
(트리 + 각 폴더 한 줄 설명)

## 5. 아키텍처 / 동작 흐름
사용자 입력 → JS fetch('/api/generate') → Vercel Python Function
→ Gemini API 호출 → JSON 응답 → 화면 렌더링

## 6. 로컬 실행 방법
​```bash
git clone https://github.com/<user>/<repo>.git
cd <repo>
npm i -g vercel        # Vercel CLI (api/ 를 로컬에서 실행하기 위해 필요)
cp .env.example .env   # 아래 7번 참고하여 키 입력
vercel dev             # http://localhost:3000
​```
> `python -m http.server` 로는 `api/` 함수가 동작하지 않습니다.

## 7. 환경 변수 설정        ← 과제 필수 항목
| 변수명 | 설명 | 발급처 |
|---|---|---|
| `GEMINI_API_KEY` | Gemini API 인증 키 | https://aistudio.google.com/apikey |

**로컬:** 프로젝트 루트에 `.env` 생성
​```
GEMINI_API_KEY=your_key_here
​```
**배포(Vercel):** Project > Settings > Environment Variables 에 동일 이름으로 추가하고
Production / Preview / Development 를 모두 체크한 뒤 재배포합니다.

> ⚠️ `.env` 는 `.gitignore` 에 포함되어 저장소에 커밋되지 않습니다.
> 키를 코드에 하드코딩하면 브라우저와 저장소 양쪽에 노출되므로 절대 금지입니다.

## 8. 배포 방법
1. GitHub 저장소 push
2. Vercel > New Project > 저장소 Import
3. **Framework Preset: Other** 선택
4. Environment Variables 에 `GEMINI_API_KEY` 등록
5. Deploy — 이후 `main` 브랜치 push 시 자동 재배포

## 9. AI 기능 명세
| 항목 | 내용 |
|---|---|
| 엔드포인트 | `POST /api/generate` |
| 요청 | `{ "input": "문자열, 1~500자" }` |
| 성공 응답 | `200 { "result": "..." }` |
| 빈 입력 | `400 { "error": "필수값을 입력하세요." }` |
| 길이 초과 | `400 { "error": "입력은 500자 이하로 입력해 주세요." }` |
| 설정 오류 | `500 { "error": "서버 설정 오류입니다..." }` |
| AI 호출 실패 | `502 { "error": "AI 응답을 가져오지 못했습니다..." }` |
| 클라이언트 타임아웃 | 20초 초과 시 지연 안내 |

## 10. 트러블슈팅 기록
(Phase 1~6에서 실제로 겪은 문제 3~5개: 증상 / 원인 / 해결)
```

> **§10을 꼭 넣어라.** 요구사항엔 없지만, 과제 목표 6번("오류 원인 파악·설명")을 문서로 증명하는 유일한 지면이다. AI가 짜준 코드를 그대로 낸 사람과 가장 확실하게 구분되는 부분이다.

### 7-2. `.env.example` 추가
```bash
echo "GEMINI_API_KEY=" > .env.example
git add .env.example && git commit -m "docs: add env example"
```
실제 키는 없고 변수명만 있는 파일. README의 실행 방법과 짝을 이룬다.

### 7-3. 제출 패키지 최종 대조표

| # | 항목 | 세부 요구 | 확인 |
|---|---|---|---|
| 1 | 배포된 웹 서비스 | 3개 이상 페이지/섹션 + 메뉴 이동 | ☐ |
| | | 모바일 정상 표시 | ☐ |
| | | AI API 연동 기능 1개 이상 (입력→출력) | ☐ |
| 2 | GitHub 저장소 | 코드 업로드됨 | ☐ |
| | | 프론트(HTML/CSS/JS)와 백엔드(api/) 구조 구분 | ☐ |
| | | 커밋 이력 존재 | ☐ |
| 3 | README.md | 서비스 소개 | ☐ |
| | | 기술 스택 | ☐ |
| | | 실행/배포 방법 | ☐ |
| | | 배포 URL | ☐ |
| | | 환경 변수 설정 방법 | ☐ |
| 4 | 서비스 기획서 | 목적 / 타겟 / 페이지 구성 / 핵심 기능 | ☐ |
| | | AI 기능의 입력·출력·실패 처리 기준 | ☐ |
| 5 | 증빙 자료 | 스크린샷 (데스크톱 + 모바일 + AI 동작) 1세트 | ☐ |
| | | AI 코딩 도구 사용 과정 1세트 | ☐ |
| — | 제약 확인 | 코드·README·스크린샷에 API 키 노출 없음 | ☐ |
| — | | 원문 예시와 아이디어·문구·구성이 다름 | ☐ |
| — | | 배포 URL에서 제3자가 재현 가능 | ☐ |

**✅ Phase 7 Done:** 위 표 전부 체크

---

## Phase 8 — 보너스 (선택, 2시간) — 여유가 있을 때만

과제 원문의 두 갈래 중 **UX 고도화가 압도적으로 효율이 좋다.** 자동화 연동(노코드)은 외부 서비스 가입·설정이 붙어 마감 리스크가 크다.

### 추천: 다크 모드 (Phase 3에서 CSS 변수를 썼다면 15~30분)
```css
:root { --bg:#fff; --fg:#111; }
[data-theme="dark"] { --bg:#111; --fg:#eee; }
body { background: var(--bg); color: var(--fg); transition: background .2s; }
```
```js
// ⚠️ 이 환경의 아티팩트에서는 localStorage 사용 불가.
//    본인 Vercel 배포에서는 사용 가능하므로, 배포 코드에는 넣어도 된다.
const saved = localStorage.getItem('theme');
const initial = saved || (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
document.documentElement.dataset.theme = initial;
```
> `prefers-color-scheme` 을 초기값으로 쓰는 이유(= 사용자 OS 설정 존중)를 설명할 수 있으면, 보너스의 *"개선 효과를 확인하는 방법"* 요구에 대한 답이 된다.

### 차선: Vercel Web Analytics (10분)
Vercel 대시보드에서 토글 한 번 + 스크립트 한 줄. 방문자 분석 요구를 최소 비용으로 충족한다.

---

## Phase 9 — 설명 가능성 자가 점검 (1시간) 🎯 **생략 금지**

과제 목표 3번은 6개 항목을 **"스스로 설명할 수 있어야 한다"** 고 요구한다. 아래 6문항에 **문서를 안 보고** 답을 써본다. 막히는 문항이 곧 이해가 빈 곳이다.

| # | 질문 | 답변에 반드시 들어가야 할 요소 |
|---|---|---|
| 1 | HTML·CSS·JS는 각각 무슨 역할인가? 내 프로젝트의 실제 파일로 예를 들어라. | 구조/표현/동작 + `index.html`·`css/style.css`·`js/app.js` 실물 매핑 |
| 2 | 내가 버튼을 눌렀을 때 결과가 화면에 뜨기까지 무슨 일이 일어나는가? | submit 이벤트 → 검증 → `fetch` POST → JSON 직렬화 → 서버리스 함수 → Gemini → JSON 응답 → `textContent` 갱신 |
| 3 | Vercel Serverless Function이란 무엇이고, 프론트가 파이썬을 어떻게 부르는가? | 요청 시에만 실행되는 서버 코드 / 상시 구동 서버 없음 / `api/generate.py` 파일 경로 = `/api/generate` 라우트 / `handler` 클래스 |
| 4 | API 키를 왜 환경변수로 두는가? 프론트에 두면 뭐가 문제인가? | 프론트 코드는 브라우저에 전부 내려감 → Network 탭·소스 보기로 노출 → 도용·과금 / Git 히스토리에 남으면 삭제가 어려움 |
| 5 | 로컬과 배포 환경은 뭐가 다른가? 배포에서만 나는 오류를 어떻게 고치는가? | `.env` vs Vercel 환경변수 / 환경변수 변경 후 재배포 필요 / Runtime Logs 확인 / push → 자동 재배포 |
| 6 | AI가 만든 코드에서 오류가 났을 때 어떻게 원인을 찾았는가? 사례 하나. | 실제 겪은 사례 (예: Flask 제안 → framework preset 감지 → api/ 무효화 → 404). 증상 → 가설 → 확인 방법 → 수정 순으로 |

> 6번은 실제 경험이 있어야 답할 수 있다. 그래서 **Phase 1부터 트러블슈팅을 기록**해두라고 한 것이다. README §10과 이 문항은 같은 재료를 쓴다.

**✅ Phase 9 Done:** 6문항 모두 문서 없이 3문장 이상 설명 가능

---

# PART 2. 리스크 대응표

## 2-1. 증상 → 원인 → 해결

| 증상 | 가장 흔한 원인 | 해결 |
|---|---|---|
| **`index.html`이 안 뜨고 404/이상한 화면** | `requirements.txt`에 flask/fastapi → framework preset 감지로 `api/` 무효화 | requirements.txt를 `google-genai` 만 남기고 재배포 |
| **`/api/generate` 404** | ①파일명 오타 ②`api/` 밖에 있음 ③`handler` 클래스명 오타(대문자 `Handler` 아님) ④framework preset 감지 | 파일 경로와 최상위 이름이 정확히 `handler` 인지 확인 |
| **로컬은 되는데 배포에서 500** | Vercel 환경변수 미등록 / Environment 체크박스 누락 / 등록 후 재배포 안 함 | Settings > Env Vars 에서 Production 체크 확인 → Redeploy |
| **`ModuleNotFoundError: google`** | `requirements.txt` 누락 또는 위치가 루트가 아님 | 루트에 `google-genai` 한 줄, 재배포 |
| **`AttributeError: module 'genai' has no attribute 'configure'`** | AI가 구버전 SDK 코드 생성 (함정 ③) | 신형 API로 교체 — 0-2 표 참조 |
| **`404 model not found`** | 모델명이 옛 문자열(`gemini-pro` 등) | AI Studio에서 현재 모델명 확인 후 교체 |
| **`429 RESOURCE_EXHAUSTED`** | 무료 티어 분당/일일 한도 초과 | 버튼 비활성화·연타 방지 확인 / 잠시 대기 / AI Studio 대시보드에서 한도 확인 |
| **응답이 깨진 한글(`ë°˜ê°`)** | 응답 헤더에 charset 없음 / `ensure_ascii` 처리 미흡 | `Content-Type: application/json; charset=utf-8` + `json.dumps(..., ensure_ascii=False)` |
| **모바일에서 반응형 안 먹음** | `<meta name="viewport">` 누락 | head에 한 줄 추가 |
| **모바일 가로 스크롤 발생** | 고정 width 요소 / 긴 URL / 이미지 | `img{max-width:100%}`, `overflow-wrap:anywhere`, DevTools로 범인 요소 탐색 |
| **배포했는데 옛 화면** | 배포 진행 중 / 브라우저 캐시 | Deployment 상태 Ready 확인 → 강력 새로고침(Ctrl+Shift+R) → 시크릿 창 |
| **fetch가 CORS 오류** | 프론트 코드가 Gemini를 직접 호출하고 있음 | 반드시 `/api/...` 를 경유. 같은 오리진이므로 CORS 자체가 발생할 이유가 없다 |

## 2-2. 🚨 API 키가 커밋됐을 때 (과제 제약사항이 명시적으로 요구하는 대응)

**순서를 지켜야 한다.**

1. **먼저 키를 폐기·재발급한다.** AI Studio에서 해당 키 삭제 → 새 키 발급 → Vercel 환경변수 교체 → 재배포. **히스토리 정리보다 이게 먼저다.** 공개 저장소는 봇이 몇 분 안에 스캔한다.
2. 그 다음 히스토리를 정리한다.
   - 커밋이 몇 개 안 되고 협업자가 없다면: **`.git` 삭제 후 저장소 새로 만드는 것이 가장 빠르고 확실하다.**
   - 이력을 살려야 한다면: `git filter-repo --path .env --invert-paths` 후 force push (`git rm --cached` 는 히스토리를 못 지운다 — 흔한 오해)
3. README나 스크린샷에 남아 있지 않은지 재확인한다.

**사전 점검 명령:**
```bash
git log -p | grep -iE "AIza|api[_-]?key\s*=\s*['\"][A-Za-z0-9_-]{20,}"
```
아무것도 안 나오면 안전.

## 2-3. 마감 압박 시 잘라낼 것 / 절대 못 자를 것

| 자를 수 있는 것 | 절대 못 자르는 것 |
|---|---|
| 보너스 과제 전체 | 배포 URL 동작 |
| 세련된 디자인·애니메이션 | 3개 이상 섹션 + 이동 |
| 이미지·일러스트 | 반응형 (모바일 표시) |
| AI 기능 2개 이상 | AI 기능 1개 (입력→출력) |
| README §10 트러블슈팅 | README 5개 필수 항목 |
| 실패 처리 3종 전부 | 실패 처리 **최소 1개** |
| | 서비스 기획서 |
| | 증빙 자료 2세트 |
| | 키 노출 0 |

---

# PART 3. 지금 바로 할 것

1. **서비스 아이디어를 알려달라.** Phase 2의 자기검열 3개(원문 예시와의 차별성 / 출력의 실용성 / 하루 내 구현 가능성)로 검토해서 약한 지점을 지적하고, 기획서 초안과 `SYSTEM_PROMPT` 문구까지 구체화해 주겠다.
2. Phase 0을 시작한다 (Gemini 키 발급 + `vercel login` + 제약 프롬프트 저장). **30분.**
3. Phase 1 워킹 스켈레톤에 착수한다. **오늘 안에 배포 URL에서 AI 왕복이 되는 것이 목표다.**

---

## 출처

- [Using the Python Runtime with Vercel Functions](https://vercel.com/docs/functions/runtimes/python) — 프레임워크 프리셋 감지, 의존성 파일, Python 버전
- [Python Functions in the /api Directory](https://vercel.com/docs/functions/runtimes/python/api-directory) — `handler` 클래스, 파일→라우트 매핑, 프리셋 우선순위
- [Vercel Functions Limits](https://vercel.com/docs/functions/limitations) — Hobby 300초·2GB·4.5MB 한도
- [Gemini API Quickstart](https://ai.google.dev/gemini-api/docs/quickstart) — `google-genai`, `GEMINI_API_KEY`, 클라이언트 초기화
- [Gemini API Text Generation](https://ai.google.dev/gemini-api/docs/text-generation) — Interactions API, `system_instruction`, `output_text`
- [Gemini API Rate Limits](https://ai.google.dev/gemini-api/docs/rate-limits) — 실제 한도는 AI Studio 대시보드에서 확인
