# 기존 자산 진단 및 재사용 전략

> 대상: 업로드 zip 3종 + GitHub 저장소 3종
> 기준: 과제 원문 6번(개발환경) / 7번(제약사항) / 2번(제출 패키지)

---

## 1. 결론 먼저

**6개 중 그대로 제출할 수 있는 것은 0개다.** 전부 과제의 개발 환경 제약을 정면으로 위반한다.

다만 **버릴 것은 껍데기(기술 스택)이고, 살릴 것은 알맹이(도메인 지식·데이터·기획)** 다. 청년 금융/정책이라는 주제 자체는 유지하되, 프론트와 백엔드는 새로 짜야 한다. 이건 손해가 아니라 오히려 유리하다 — 이유는 §4에서 설명한다.

---

## 2. 자산별 판정

| # | 자산 | 실제 내용 | 판정 | 치명적 사유 |
|---|---|---|---|---|
| 1 | `GunSan-youth-dashboard-KOSIS` | Streamlit 기반 FinFit MVP (Pandas, Plotly, SQLite, pytest, 온통청년 API) | ❌ 사용 불가 | **Streamlit은 프레임워크다.** 과제 6번 "프론트엔드는 순수 HTML/CSS/JavaScript" 위반. 프론트/백 구분도 불가능 |
| 2 | `Youth_Platform_Trends_and_Business_Reports` | Streamlit + 리포트 문서 | ❌ 사용 불가 | 동일. 게다가 "웹 서비스"가 아니라 **리포트**다. 과제 결과물 정의와 다름 |
| 3 | `wongchour_finfit_prototype1` | Vercel 배포됨. `index.html`(Figma Make 산출 목업) + `app.py`(Streamlit, 실행 안 됨) + `Requirements.txt` | ⚠️ 부분 참고 | ①`index.html`이 **Figma Make 자동 생성물** — "본인 손으로 만든" 증빙이 안 됨 ②`api/` 폴더 없음 ③**AI API 연동 0%** ("GPT 쉬운말 요약"은 화면 속 문구일 뿐 실제 호출 없음) |
| 4 | `new.zip` (금융 정보 플랫폼 데모2) | Figma Make export. React 18 + Vite + Tailwind + shadcn/ui + MUI + Recharts, 18개 페이지 | ❌ 사용 불가 | 과제 6번이 **"React/Vue 등 프레임워크 사용 금지"** 를 명시. 이 프로젝트는 그 자체가 React 앱이다. 변환이 아니라 폐기 대상 |
| 5 | `web1.zip` | Flutter 프로젝트 | ❌ 사용 불가 | 웹 과제가 아니다. 게다가 `lib/main.dart`가 **Flutter 기본 카운터 데모 그대로**이고, `pubspec.yaml`에 `http`·`fl_chart`만 추가된 상태. 실질 진행도 0 |
| 6 | Kunsan Dormitory auto-check (세션 로그) | Grok/cursor 세션 기록. Playwright + Cloud Run + E2EE + Firestore 백엔드 설계 | ❌ 주제 불일치 / ⭕ **증빙 자료로는 유용** | Vercel Serverless Python 구조가 아님. 다만 320개 대화 메시지는 §5 참고 |

---

## 3. 반드시 짚어야 할 것 3개

### 3-1. `wongchour_finfit_prototype1`이 Vercel에서 뜨는 건 **우연이다**

이 저장소의 의존성 파일 이름은 `Requirements.txt` — **대문자 R**이다. 내용은 `streamlit>=1.32.0`.

Vercel의 리눅스 빌드는 대소문자를 구분하므로 `requirements.txt`를 찾지 못한다. 그래서 Streamlit이 감지되지 않고, framework preset이 발동하지 않아서, `index.html`이 정적 파일로 그냥 서빙된다.

**만약 파일명을 소문자로 고쳤다면 그 사이트는 즉시 깨졌을 것이다.** 앞서 계획서에서 "함정 ①"로 경고했던 그 현상이 본인 저장소에서 이미 재현 직전 상태였다는 뜻이다. 이번 과제에서 `requirements.txt`(소문자)에 `google-genai`만 넣어야 하는 이유가 여기 있다.

추가로 루트의 `app.py`는 Vercel이 **엔트리포인트 파일명으로 인식하는 이름**이다. 만약 그 파일이 최상위 `app` (ASGI/WSGI) 변수를 정의했다면 그것도 프리셋을 유발했을 수 있다. 지금 구조는 우연이 두 겹 겹쳐 굴러가는 상태다.

### 3-2. 저장소를 **새로 파야 한다** — 커밋 이력 때문에

과제 4번은 *"GitHub 저장소를 생성하고 커밋 이력을 남긴다"* 를 요구한다. 여기서 커밋 이력은 **작업 과정의 증거**다.

그런데 기존 저장소들의 상태는:
- `GunSan-youth-dashboard-KOSIS`: 1,333 커밋
- `Youth_Platform_Trends_and_Business_Reports`: 352 커밋, `streamlit_reboot_macro` 폴더 + GitHub Actions
- `wongchour_finfit_prototype1`: 348 커밋, `reboot_trigger.txt` + GitHub Actions

이 조합(트리거 파일 + Actions + 수백 커밋)은 **Streamlit Cloud 슬립 방지용 자동 커밋**으로 보인다. 맞다면, 이 저장소에 과제 코드를 얹는 순간 **실제 작업 커밋 20~30개가 자동 커밋 수백 개에 묻힌다.** 평가자 입장에서 과정이 안 보인다.

→ **이번 과제는 빈 저장소에서 시작한다.** 계획서 Phase 1의 `.gitignore` 우선 커밋부터 그대로 따라가면 된다. 커밋 20~30개가 전부 사람이 짠 흔적인 저장소가, 1,300개짜리 저장소보다 이 과제에서는 훨씬 강하다.

### 3-3. 진짜 문제는 기술이 아니라 **완주 패턴**이다

같은 아이디어(청년 금융/정책 플랫폼)를 최소 4번 다시 시작했다.

```
Streamlit (KOSIS 대시보드)  →  Streamlit (트렌드 리포트)
      →  Figma Make React (데모2, 18페이지)
      →  Figma Make 정적 목업 (Vercel 배포)
      →  Flutter (web1, 카운터 데모에서 멈춤)
```

커밋 수는 2,000개가 넘는데, **입력을 받아 AI를 호출해 결과를 돌려주는 서비스는 하나도 없다.** 스택을 바꿀 때마다 처음부터 다시 시작했기 때문이다.

며칠 안에 끝내야 하는 이번 과제에서 가장 큰 리스크는 Vercel도 Gemini도 아니라 **이 패턴의 반복**이다. 다섯 번째 재시작이 되면 안 된다.

계획서의 Phase 1(워킹 스켈레톤 — 못생긴 HTML 한 장으로 배포부터 성공)이 정확히 이걸 막으려는 장치다. **화면을 예쁘게 만들기 전에 AI 왕복이 배포 URL에서 도는 것을 먼저 확보한다.** Figma Make로 18페이지를 먼저 뽑는 순서로 가면 또 같은 결과가 난다.

---

## 4. 살릴 것 / 버릴 것

| 살린다 (자산) | 버린다 (부채) |
|---|---|
| 청년 금융·정책 **도메인 이해** | Streamlit / React / Flutter 코드 전부 |
| 혜택 4대 카테고리 분류 (자산형성/주거/취업/생활) | Figma Make 자동 생성 컴포넌트 |
| 온통청년 API·KOSIS 데이터 **조사 결과** | 18페이지 화면 구성 (과제엔 3섹션이면 충분) |
| "복잡한 정책을 쉬운 말로" 라는 **핵심 문제 정의** | 온보딩 3단계, 가계부, 로그인, 알림 등 부가 기능 |
| FinFit 기획서에 이미 있는 **타겟·문제 서술** | 기존 저장소의 커밋 이력 |
| 군산이라는 **지역 특수성** | Firestore/Cloud Run/Playwright 백엔드 설계 |

> **화면 디자인 참고는 해도 된다.** `wongchour_finfit_prototype1`의 목업을 보면서 **직접 HTML/CSS로 새로 짜는 것**은 문제없다. 그대로 복붙하는 것이 문제다. 과제 제약 *"템플릿/예시를 그대로 복제하지 않는다"* 는 원문 예시(멍냥케어)를 겨냥한 조항이지만, 자동 생성물을 그대로 내는 것도 "본인이 만든 것"으로 보기 어렵다.

---

## 5. 뜻밖의 수확 — 증빙 자료가 이미 있다

`Kunsan-University-Dormitory-auto-check` 세션 로그는 과제 주제와는 무관하지만, **AI 코딩 도구 사용 증빙**으로서의 성격이 강하다.

- `prompt_history.jsonl` — 실제 프롬프트와 타임스탬프
- `chat_history.jsonl` — 320개 대화 메시지
- `terminal/*.log` — 실행 로그
- 특히 *"파일로는 뜨는데, 왜 터미널에 복원되지는 않는 건가?"*, *"그럼 왜 날라갔나?"* 같은 **원인을 캐묻는 프롬프트**들이 남아 있다

과제 목표 6번은 *"AI 코딩 도구로 코드를 생성하더라도, 오류 원인을 파악하고 수정 방향을 말로 설명할 수 있다"* 다. 위와 같은 대화가 정확히 그 증거 형식이다.

**단, 이번 과제 증빙은 이번 프로젝트에서 나온 것이어야 한다.** 위 로그는 "나는 원래 이런 식으로 AI와 대화한다"는 참고일 뿐, 제출물로 쓰면 주제 불일치다. **이번 프로젝트에서 같은 밀도로 기록을 남기면 된다** — 그게 이미 습관이 되어 있다는 게 확인된 셈이다.

⚠️ 다만 제출 전 **로그에 API 키·토큰·비밀번호가 섞여 있지 않은지 반드시 검사**해야 한다. 터미널 로그와 세션 파일은 키가 가장 잘 새는 곳이다.

---

## 6. 아이디어 축소안 — 3일 안에 끝나는 형태로

### 6-1. FinFit을 그대로 하면 안 되는 이유

기존 FinFit 기획의 범위:
> 온보딩 3단계 → 프로필 → 혜택 매칭 엔진 → 가계부 → 저축 단계 10레벨 → KOSIS 대시보드 → 정책 요약 → 알림 → 설정 → FAQ ... (18페이지)

이건 3개월짜리 제품이다. 3일에 넣으면 **또 미완성으로 끝난다.** 과제가 요구하는 건 **AI 기능 1개**와 **섹션 3개**다.

또 하나: "프로필 입력 → 혜택 추천" 구조는 과제 원문 예시(**반려동물 종류/나이/기간 입력 → 추천 서비스 출력**)와 **구조가 동일하다.** 제약사항이 *"아이디어·문구·구성 변경 필수"* 라고 못 박은 지점에 정통으로 걸린다. 도메인만 바꾼 복제로 읽힐 위험이 있다.

### 6-2. 권장안 — "정책 공고문 → 쉬운 말 변환기"

FinFit 기획서에 이미 들어 있던 *"공공 API → 카테고리 분류 → GPT 쉬운말 요약"* 중 **요약 조각 하나만 떼어내 독립 서비스로 만든다.**

| 항목 | 내용 |
|---|---|
| **입력** | 청년 정책 공고문 원문을 **붙여넣기** (자유 텍스트, 1~2,000자) |
| **출력** | ① 3줄 요약 ② **내가 대상인지 확인하는 체크리스트** ③ 준비 서류 목록 ④ 신청 기한 |
| **사용자 가치** | 공고문 PDF를 3번 읽어도 "그래서 내가 되는 건가?"가 안 풀리는 문제를 1회 붙여넣기로 해결 |
| **AI 호출** | 1회. 구조화 출력을 프롬프트로 지시 |

**왜 이 안인가**

1. **원문 예시와 구조가 다르다.** 예시는 *드롭다운 몇 개 → 추천*, 이건 *긴 텍스트 → 구조화된 변환*. 복제 논란이 없다.
2. **입력이 자유 텍스트라 테스트 케이스가 자연스럽게 나온다.** 과제 8번이 요구하는 "빈 입력 / 200자 이상 긴 입력" 시나리오가 억지 없이 만들어진다.
3. **DB도 API 연동도 필요 없다.** 온통청년 API 붙이면 인증·쿼터·응답스키마로 하루가 날아간다. 이번엔 사용자가 텍스트를 직접 붙여넣는다. 3일 안에 확실히 끝난다.
4. **출력이 실용적이다.** "추천 문구 한 줄"이 아니라 체크리스트와 서류 목록이라 사용자가 다음 행동을 할 수 있다. 기획 검열 기준 ②를 통과한다.
5. **기존 조사 자산이 그대로 쓰인다.** 4대 카테고리 분류, 군산 청년 정책 지식이 프롬프트 설계와 예시 데이터에 들어간다.

**섹션 3개 구성안**

| # | 섹션 | 내용 |
|---|---|---|
| 1 | 홈 | 문제 제기 (공고문이 왜 안 읽히는가) + 바로 쓰기 CTA |
| 2 | **변환기** (AI) | 붙여넣기 → 4블록 결과. 예시 공고문 "샘플 넣어보기" 버튼 |
| 3 | 이용 안내 / 한계 | 사용법, AI 요약의 한계와 원문 확인 안내, 군산 청년 정책 링크 |

> 3번 섹션에 **"이 결과는 참고용이며 최종 확인은 반드시 공고 원문으로"** 를 명시할 것. 기존 FinFit README의 Non-Goals 항목(*법적 판단·투자 조언 배제*)과 같은 태도이고, 금융·정책 도메인에서는 이게 신뢰도를 높인다.

### 6-3. 대안 (이 안이 마음에 안 들 경우)

| 안 | 입력 → 출력 | 위험 |
|---|---|---|
| B. 지원서 자기소개 문장 다듬기 | 초안 텍스트 → 개선안 + 수정 이유 | 흔한 형태. 차별성 약함 |
| C. 청년 지원금 용어 사전 | 어려운 용어 → 쉬운 설명 + 예시 | 입력이 짧아 실패 테스트 케이스 만들기 빈약 |
| D. 혜택 조건 매칭 추천 | 나이/소득/거주 → 추천 목록 | **원문 예시와 구조 동일. 권하지 않음** |

---

## 7. 확정해야 할 것

1. **주제를 청년 금융/정책으로 유지할 것인가?** (기존 조사 자산을 살리려면 유지가 유리)
2. **AI 기능을 §6-2 "공고문 → 쉬운 말 변환기"로 갈 것인가, B/C 중 다른 것인가?**
3. 서비스명 (FinFit 재사용 가능하지만, 새 이름이면 기존 프로토타입과의 혼동이 없다)

위 3개가 정해지면 **기획서 초안 + `SYSTEM_PROMPT` 문구 + 섹션별 HTML 골격**까지 바로 작성한다.
